# src/

Writable source tree for the new unified **Yandex Marketing Bridge**.

Do not modify files under `extension/reference/` in place to create the new product.

Phase 0/1 will populate this directory by extracting/adapting proven CORE behavior from the Wordstat Bridge 1.1.5 reference and then implementing service adapters one phase at a time.
