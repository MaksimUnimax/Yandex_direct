# KW-002 — 00 READ RULES BEFORE ANY WORK/JOB ACTION

Status: **ACTIVE / OWNER-LOCKED / FAIL-CLOSED**

Before doing ANY material action under `work/`, read in full the current live authority beginning with:

`../LEVEL1/00_MANDATORY_FULL_RULE_REREAD_NO_ACTION_GATE.md`

Then read all applicable Level1/Level2 rules, current job `JOB_FLOW.md`, current cursor, accepted manifests/evidence, and relevant failure ledger.

Known recurring assistant failure:

```text
The assistant has repeatedly violated explicit rules when it relied on memory, prior summaries or technical artifacts instead of freshly reading the live rule set in full.
```

Therefore:

```text
NO FULL RULE REREAD
→ NO JOB PREPARATION
→ NO ANALYSIS
→ NO WORK HANDOFF
→ NO PROVIDER / BRIDGE ACTION
→ NO PUBLICATION ACCEPTANCE
→ NO CURSOR MOVEMENT
```

For every major owner-facing report, `PRE_STEP_EXTERNAL_RESEARCH_AND_SOURCE_DISCLOSURE_RULE.md` must be freshly read and obeyed. The chat itself must contain clickable studied-source links, what each source supports, source→method trace and a normal Russian `ПРОСТЫМИ СЛОВАМИ` summary. Artifact-only disclosure does not pass.
