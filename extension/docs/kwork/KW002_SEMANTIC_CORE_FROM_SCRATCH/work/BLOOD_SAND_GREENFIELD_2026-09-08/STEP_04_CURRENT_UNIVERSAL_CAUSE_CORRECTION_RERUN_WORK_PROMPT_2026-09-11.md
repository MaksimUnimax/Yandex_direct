# KW-002 — W09 Step04 current-authority universal-cause full-volume rerun

Date: 2026-09-11
Status: **CANONICAL WORK PROMPT / CURRENT-AUTHORITY RERUN / STEP05 BLOCKED**

Continue the existing KW-002 job.

THIS IS NOT A NEW PROJECT.
THIS IS NOT STEP05.
THIS IS NOT STEP06.
THIS IS NOT A PROVIDER-ACQUISITION TASK.
THIS IS A FULL-VOLUME STEP04 CORRECTION/VERIFICATION RERUN AFTER W08 WAS REJECTED FOR STALE-BASE AUTHORITY DRIFT.

Repository:
`MaksimUnimax/Yandex_direct`

Branch:
`roadmap/kwork-productization-2026-08-28`

Job root:
`extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH/work/BLOOD_SAND_GREENFIELD_2026-09-08`

## 0. First hard gate — CURRENT REMOTE BASE, not cached checkout

Before reading job conclusions or executing code:

1. `git fetch` / otherwise fetch the CURRENT remote branch;
2. record the actual remote branch HEAD;
3. make the execution workspace reflect that current branch without force-overwriting unrelated work;
4. verify the mandatory current authority files listed below exist on that fetched head;
5. record their blob/file identity where practical.

Hard rule:

```text
CACHED_LOCAL_BRANCH != CURRENT AUTHORITY
```

If the current remote branch cannot be fetched or mandatory current authority is missing:

```text
STOP
WORK_EXECUTION_ALLOWED = false
```

Do not silently execute from an older local checkout.

## 1. Read universal authorities IN FULL

Level1:

- `LEVEL1/ROADMAP_AND_METHOD_GENERALIZATION_RULE.md`
- `LEVEL1/WORK_BASE_FRESHNESS_AND_AUTHORITY_DRIFT_RULE.md`
- `LEVEL1/EXECUTION_FAILURE_LEDGER_AND_ANTI_REGRESSION_RULE.md`
- `LEVEL1/DATA_VOLUME_SANITATION_AND_DELIVERY_SCOPE_RULE.md`
- `LEVEL1/JOB_DATA_SEPARATION_AND_LIFECYCLE.md`
- `LEVEL1/PRE_STEP_EXTERNAL_RESEARCH_AND_SOURCE_DISCLOSURE_RULE.md`
- `LEVEL1/RESULT_QUALITY_SCORING_RULE.md`
- `LEVEL1/WORK_HANDOFF_RULE.md`

Level2:

- `LEVEL2/README.md`
- `LEVEL2/STEP_RULES_INDEX.md`
- `LEVEL2/STEP_04_PRELIMINARY_FAMILY_TRIAGE_QUALITY_GATE.md`

These authorities define the method for ANY site. Concrete current-job PSF/query examples do not define the method.

## 2. Read current job authorities IN FULL

At minimum:

- current `KW002_EXECUTION_CURSOR_2026-09-11.json` after Main ChatGPT restores it;
- `STEP_04_INDEPENDENT_FULL_VOLUME_RESULT_AUDIT_MAIN_CHATGPT_REVIEW_2026-09-11.md`;
- `STEP_04_POST_AUDIT_CORRECTED_MAIN_CHATGPT_REMOTE_REVIEW_2026-09-11.md`;
- W07 report/overlay/metrics/family/boundary/queue-feedback/regression authorities;
- W08 corrected payload/QA/materializer/transition/queue/feedback/independent QA as **historical candidate/regression evidence only**;
- current accepted Step03A and corrected Step03B authorities;
- current job failure ledger and Step04 audit addendum;
- allowed/sealed source authority.

Do NOT treat the W08 local PASS candidate as current accepted authority.

## 3. Frozen semantic/accounting boundary

Current accepted upstream universe remains:

```text
NORMALIZED_IDENTITIES = 24576
RAW_OCCURRENCES = 25979
KEEP = 5100
HOLD = 13035
EXCLUDE = 6441
ACTIVE_PLUS_HOLD = 18135
```

These are job facts, not universal constants.

Hard boundaries:

```text
STEP03A_MUTATION_ALLOWED = false
STEP03B_MUTATION_ALLOWED = false
WORDSTAT_CALLS_ALLOWED = 0
ORDINARY_SEARCH_CALLS_ALLOWED = 0
GENSEARCH_CALLS_ALLOWED = 0
AI_SEARCH_CALLS_ALLOWED = 0
SEALED_PRIOR_RESEARCH_ALLOWED = false
STEP05_ADVANCEMENT_ALLOWED = false
STEP06_ADVANCEMENT_ALLOWED = false
FINAL_INTENT_ALLOWED = false
SERP_CLUSTERING_ALLOWED = false
QUERY_TO_PAGE_ALLOWED = false
SITE_IA_ALLOWED = false
```

## 4. What W09 must prove — universal causes, not known fixtures

### U1. Lexical boundary safety

No unbounded stem/prefix/substr rule may be conclusive semantic proof when sibling lexemes can collide.

Required generic gate:

```text
UNBOUNDED_PREFIX_OR_SUBSTRING_AS_SEMANTIC_PROOF = 0
```

Known current-job collision examples remain regression fixtures only.

### U2. Taxonomy-wide specificity / precedence contract

Do not rely on arbitrary first-match ordering.

For each active/HOLD identity, derive simultaneous material signals before final preliminary primary-family routing, or prove an equivalent explicit specificity contract.

At minimum distinguish dimensions where present:

```text
topic/context
object/form
user action/task
commercial action
foreign referent/collision
information/meaning task
media/digital task
visual task
make/craft task
other discovered coherent task
```

The exact vocabulary/classes may adapt to the data. This list is not a closed taxonomy.

Hard generic gate:

```text
EXPLICIT_TASK_HIDDEN_BY_BROADER_TOPIC = 0
RULE_PRECEDENCE_CONTRACT = PRESENT
```

A primary family may still be chosen for compact triage, but materially stronger secondary task/context signals must remain auditable.

### U3. Open-taxonomy / unknown-task discovery

The production taxonomy must be challenged for coherent tasks it did not already know about.

Do not merely test the known W07 DIY/toy/zodiac cases.

Run a discovery-oriented full-volume diagnostic over large/generic/residual families using a method not equivalent to the production classifier. Combine, where useful:

- action/task phrase extraction independent of current family labels;
- term/co-occurrence analysis;
- TF-IDF/topic decomposition;
- semantic similarity/embedding diagnostics if available without external provider calls;
- stratified review of suspicious subgroups.

Required generic gate:

```text
REPEATED_EXPLICIT_TASK_PATTERN_HIDDEN_IN_GENERIC = 0
UNKNOWN_TASK_DISCOVERY_ROUTE = PRESENT
LARGE_GENERIC_FAMILY_HETEROGENEITY_AUDIT = PASS
```

If a new coherent task family/marker appears, model it as preliminary evidence. Do not suppress it to preserve the current family count.

### U4. Independent taxonomy challenge

The corrected classifier cannot be its own semantic judge.

The independent diagnostic must be capable of finding defects outside predefined W07 classes.

Required proof:

```text
INDEPENDENT_DIAGNOSTIC_DEFECT_LOGIC_NOT_LIMITED_TO_KNOWN_PSF_IDS = true
INDEPENDENT_FAMILY_COHERENCE_DIAGNOSTIC = PASS
```

Similarity/topic metrics are diagnostic only, not final SERP clusters.

### U5. Global evidence reconciliation before expansion

Every retained Step05 queue hypothesis must be checked against ALL durable evidence, not only its local Step04 family.

Required gates:

```text
PROVIDER_READY_WITH_EQUIVALENT_DURABLE_EVIDENCE = 0
OWNER_FACT_GAP_SENT_TO_PROVIDER = 0
INCREMENTAL_INFORMATION_GAIN_EXPLICIT = true where provider-eligible later
NEGATIVE_RESULT_VALUE_EXPLICIT = true where provider-eligible later
STOP_CONDITION_EXPLICIT = true
```

No provider call in this run.

## 5. Required identity-level signal authority

Materialize a complete identity-level audit authority, separate from or embedded in the transition ledger, containing at least:

```text
normalized_phrase_id
canonical_phrase
step03b_state
step03b_reason
primary_preliminary_family_id
primary_assignment_reason
all_material_detected_signals / secondary_task_context_signals
specificity_or_precedence_explanation
ambiguity_state
later_evidence_needed
old_step04_family
w08_candidate_family_if useful
```

For EXCLUDE history, preserve accounting without re-triaging.

This authority exists so a reviewer can prove that a broad primary label did not erase a stronger secondary task.

## 6. Full-volume rerun

Rerun all 24,576 normalized identities under the current universal method.

For all 18,135 active/HOLD identities:

- recompute preliminary primary family/task assignment;
- preserve simultaneous/secondary material signals;
- materialize deterministic assignment reason;
- materialize old/current-candidate -> new transition;
- report every movement, including changes beyond W07/W08 known fixtures.

Rematerialize all 25,979 RAW occurrence links.

Required:

```text
RAW_LINEAGE_LOSS = 0
UNASSIGNED_RAW_OCCURRENCES = 0
UNEXPECTED_DUPLICATE_RAW_OCCURRENCES = 0
STEP03B_MUTATIONS = 0
```

Family count is emergent.

## 7. W07/W08 fixtures remain mandatory regression evidence

All accepted known defects must remain corrected:

- known toy/game prefix collision class;
- known broad-topic explicit-task leakage class;
- known make/craft generic-fallback class;
- known queue duplicate/satisfied evidence cases;
- all older Step03B/Step04 lexical collision regressions.

But passing these fixtures is necessary, not sufficient.

## 8. Family authority boundary

Each preliminary family must make clear:

```text
family definition
in/out boundary
business relevance confidence
family coherence confidence
primary user-task hypothesis
secondary task/context coverage or link to identity signal authority
ambiguity class
coverage state
later evidence needed
final_intent_status = NOT_FINAL_AT_STEP04
serp_cluster_status = NOT_EVALUATED_AT_STEP04
frequency_used_for_semantic_assignment = NO
```

Step04 family != page.

## 9. Queue and feedback rebuild

Rebuild the Step05 expansion queue from the corrected authority, but do not execute Step05.

Rebuild sanitation/family feedback without mutating Step03B.

Distinguish:

```text
UPSTREAM_SANITATION_FINDING
STEP04_ROUTING_FINDING
OWNER_FACT_GAP
VALID_INCREMENTAL_SEARCH_GAP
EXISTING_EVIDENCE_REUSE
DEFER_TO_LATER_SERP/INTENT
```

## 10. Fresh methodology/source check

Revalidate the current Step04 methodology against the same-day/current sources required by Level1. If nothing material changed since the immediately preceding same-day research, record explicit revalidation rather than pretending a new source search discovered something different.

No provider call is allowed.

## 11. Required outputs

Materialize new, non-overwriting current-authority files, at minimum:

1. `STEP_04_CURRENT_AUTHORITY_OCCURRENCE_FAMILY_LEDGER_2026-09-11.tsv`
2. `STEP_04_CURRENT_AUTHORITY_FAMILY_TRIAGE_2026-09-11.tsv`
3. `STEP_04_CURRENT_AUTHORITY_IDENTITY_SIGNAL_LEDGER_2026-09-11.tsv`
4. `STEP_04_CURRENT_AUTHORITY_TARGETED_EXPANSION_QUEUE_2026-09-11.tsv`
5. `STEP_04_CURRENT_AUTHORITY_SANITATION_FEEDBACK_REGISTER_2026-09-11.tsv`
6. `STEP_04_CURRENT_AUTHORITY_TRANSITION_LEDGER_2026-09-11.tsv`
7. `STEP_04_CURRENT_AUTHORITY_KNOWN_FAILURE_REGRESSION_MATRIX_2026-09-11.tsv`
8. `STEP_04_CURRENT_AUTHORITY_INDEPENDENT_TAXONOMY_AUDIT_2026-09-11.tsv` or equivalent full-volume audit authority
9. `STEP_04_CURRENT_AUTHORITY_QA_2026-09-11.md`
10. `STEP_04_CURRENT_AUTHORITY_WORK_RETURN_2026-09-11.md`
11. `STEP_04_CURRENT_AUTHORITY_ARTIFACT_MANIFEST_2026-09-11.json`
12. deterministic materializer/source used for this run
13. source-revalidation trace if not embedded in QA/return.

Historical W07/W08 files must remain untouched.

Do NOT write current mutable cursor/JOB_FLOW/JOB_MANIFEST into an owner-relay ZIP from a stale or long-running workspace. Main ChatGPT will update current state after accepting the remote payload, unless Work re-fetches/reconciles the remote head immediately before packaging.

## 12. Quality scoring

Compute a fresh score using the current universal quality rule. Do not inherit 97.20, 67.69 or 96.94.

A score cannot override any failed hard gate.

## 13. Pre-publication remote-head gate

Immediately before push or owner-relay package creation:

1. fetch/check the remote branch HEAD again;
2. record `WORK_PRE_PUBLICATION_REMOTE_HEAD`;
3. compare it with `WORK_START_REMOTE_HEAD`;
4. if remote advanced, classify changed paths;
5. if any governing Level1/Level2/prompt/release/upstream authority changed, revalidate/rerun as necessary;
6. reconcile mutable state files or exclude them from owner-relay payload.

Hard gate:

```text
STALE_BASE_MUTABLE_STATE_FILE_OVERWRITE = 0
```

## 14. Publication

Use native Git if already authenticated. Otherwise use approved owner relay.

Owner relay must include exact analytical payload files and optional transport ZIP. It must state the exact target folder and commit message.

Remote readback by Main ChatGPT remains mandatory.

## 15. Stop

STOP after current-authority Step04 artifacts, independent QA and publication/owner-relay preparation.

Do NOT start/resume Step05.
Do NOT start Step06.
Do NOT make provider calls.
