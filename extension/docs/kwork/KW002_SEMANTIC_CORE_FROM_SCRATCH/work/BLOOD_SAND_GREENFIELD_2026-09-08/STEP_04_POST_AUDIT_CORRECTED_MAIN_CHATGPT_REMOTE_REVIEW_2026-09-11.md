# KW-002 Blood & Sand — Main ChatGPT remote review of W08 corrected Step04 return

Date: 2026-09-11
Status: **REMOTE PUBLICATION READBACK PASS / DATA MATERIALLY IMPROVED / CURRENT V2 CONTRACT NOT ACCEPTED / STEP04 REWORK REQUIRED**

## 1. Publication readback

Owner upload commit:

`7ab817d80457e4124f62d9248bb60d5cc2f13fd0`

Parent/current branch state immediately before owner upload:

`90f64dffc42fb97b6101064e5492d8876b9a8f91`

The upload is one commit above that parent and contains exactly 17 expected job-root paths. No unrelated path was introduced by the owner upload.

Therefore:

```text
OWNER_UPLOAD = PASS
17_FILE_PATH_SET = PASS
REMOTE_PUBLICATION_PRESENCE = PASS
```

This publication PASS does not by itself make the analytical result current authority.

## 2. Critical authority-version defect

The uploaded W08 artifacts explicitly record:

```text
WORK_LIVE_BASE_HEAD = 5446a9347cac33da6a656e9db7c252e1b529295a
```

But before W08 V2 execution the branch had already advanced to the universal-method refactor state and current V2 release at/through:

`90f64dffc42fb97b6101064e5492d8876b9a8f91`

The artifact manifest and corrected materializer also freeze the older W08 prompt/release as execution inputs rather than the later V2 universal-cause prompt/release.

This proves that Work did not execute the current authority snapshot.

Consequences:

1. W08 cannot be accepted against the current V2 execution contract merely because its old-contract QA passes.
2. The owner relay overwrote the newer V4 execution cursor with a stale V3 current-state file.
3. The universal-method refactor remains valid remote history, but the uploaded cursor no longer represents current authority and must be restored/rebuilt.

This incident is now generalized by Level1 `WORK_BASE_FRESHNESS_AND_AUTHORITY_DRIFT_RULE.md`.

## 3. What is genuinely improved in the W08 analytical payload

The W08 data is not discarded. Remote inspection confirms substantial rule-level correction rather than a 255-row manual patch.

### Full-volume accounting

```text
NORMALIZED_IDENTITIES = 24576
RAW_OCCURRENCES = 25979
ACTIVE_PLUS_HOLD = 18135
STEP03B_MUTATIONS = 0
RAW_LINEAGE_LOSS = 0
```

### Known W07 defect fixtures

The corrected materializer replaces the broad game prefix with bounded game morphology, separates toy morphology, adds bounded make/craft detection, and reruns the full universe.

The published QA reports:

```text
W07_DEFECT_IDENTITIES_CORRECTED = 255 / 255
TOTAL_CHANGED_IDENTITIES = 357
ADDITIONAL_RULE_BLAST_RADIUS_CHANGES = 102
```

The additional movements are evidence that the correction was not merely a hard-coded list of known row IDs.

### Current corrected taxonomy candidate

Published candidate:

```text
FAMILIES = 28
OBSERVED = 26
COVERAGE_GAPS = 2
QUEUE_ROWS = 9
FEEDBACK_ROWS = 13
```

The queue has no row marked provider-ready in the current blocked state, and previously identified duplicate/satisfied items are removed or narrowed. Historical durable evidence is not authorized for blind replay.

### Published old-contract QA

```text
KNOWN_FAILURE_REGRESSIONS = 24 / 24 PASS
INDEPENDENT_FAMILY_QA_ROWS = 28
INDEPENDENT_QA_CRITICAL_DEFECT_ROWS = 0
```

These facts make the W08 payload useful historical/candidate evidence for the next run.

## 4. Why current V2 acceptance still fails

The current V2 contract is broader than the old W08 correction contract.

It requires universal root-cause proof, not only the known site-specific fixtures.

### 4.1 Start-base freshness — FAIL

```text
CURRENT_REMOTE_AUTHORITY_AT_RELEASE != W08_RECORDED_LIVE_BASE
```

This alone blocks current acceptance.

### 4.2 Current prompt/release authority — FAIL

The W08 manifest/materializer identify the superseded non-V2 prompt and release as frozen execution inputs. They do not prove that the current universal Level1/Level2 method was read and executed.

### 4.3 Generic precedence gate — NOT PROVEN

V2 requires:

```text
EXPLICIT_TASK_HIDDEN_BY_BROADER_TOPIC = 0
RULE_PRECEDENCE_CONTRACT = PRESENT
```

across the complete taxonomy.

The W08 regression matrix proves the known zodiac/task cases, but the independent critical-defect logic remains explicitly centered on the previously known PSF001/PSF014/PSF019 classes. It does not establish a taxonomy-wide generic test for every broad-topic/stronger-task conflict.

### 4.4 Generic-fallback unknown-task discovery — NOT FULLY PROVEN

W08 successfully discovered/routed the known DIY class and additional sibling rows. However, the current V2 contract requires a reusable challenge that can detect an *unknown* coherent task not already represented in the hand-built signal vocabulary.

TF-IDF/topic diagnostics are present and useful, but the published acceptance logic does not prove that every material repeated explicit task hidden in a generic family would be surfaced as a blocking finding.

### 4.5 Secondary-signal preservation — INSUFFICIENTLY MATERIALIZED

V2 requires broad topic/context not to erase stronger task information and recommends preserving secondary task/context signals as auditable metadata. The W08 family authority still primarily exposes one family assignment and does not provide a complete identity-level primary+secondary signal authority sufficient to prove the universal precedence contract independently.

### 4.6 Mutable state overwrite — FAIL

The owner relay included a stale current execution cursor and overwrote the newer current-state representation. Analytical payload publication and mutable state publication were not safely separated.

## 5. Main ChatGPT quality score for the current W08 return

This score evaluates the return against the **current V2 contract**, not the older local contract.

| Dimension | Score / 10 | Reason |
|---|---:|---|
| goal/output completeness | 9.5 | substantial full-volume corrected payload exists |
| method/source support | 7.0 | execution used superseded prompt/release snapshot |
| input/provenance integrity | 10.0 | upstream Step03B and RAW lineage preserved |
| coverage/completeness | 10.0 | full normalized/RAW accounting present |
| analytical correctness/claim boundaries | 8.0 | known defects corrected, universal precedence proof incomplete |
| adversarial QA quality | 7.0 | independent diagnostics exist but blocking logic remains known-class-centric |
| persistence/readback/reproducibility | 8.0 | payload readback passes; stale mutable state regression occurred |
| owner/client usability | 8.5 | return is understandable but authority version was misleading |
| execution efficiency/information gain | 9.0 | no provider calls; correction reused preserved evidence |
| downstream readiness | 3.5 | Step05 cannot safely resume under current V2 authority |

```text
QUALITY_TOTAL = 80.5 / 100
QUALITY_SCORE = 8.05 / 10
CURRENT_V2_VERDICT = REWORK_REQUIRED
```

Hard authority-version gates fail, so the numerical score cannot produce PASS.

## 6. Current authority status

```text
W08_REMOTE_PUBLICATION = PASS
W08_KNOWN_DEFECT_CORRECTION_EVIDENCE = STRONG / PRESERVED
W08_CURRENT_V2_ACCEPTANCE = FAIL / REWORK_REQUIRED
STEP04_CURRENT_SEMANTIC_AUTHORITY = NOT_READY
STEP05_ALLOWED = false
STEP06_ALLOWED = false
NEW_PROVIDER_CALLS = 0
```

Do not delete or rewrite the W08 payload. It remains historical correction evidence and may be used as comparison/regression input in the current rerun.

## 7. Required next action

Run a new full-volume Step04 correction/verification pass from the **current remote branch**, using all current universal Level1/Level2 authorities.

The new pass must:

1. prove start-base freshness;
2. use current universal-cause prompt/release only;
3. rerun all 24,576 normalized identities and rematerialize all 25,979 RAW links;
4. materialize simultaneous/secondary task-context signals at identity level or an equivalent auditable representation;
5. apply a generic specificity/precedence contract across the whole taxonomy;
6. independently challenge large/generic families for unknown coherent tasks, not only known W07 classes;
7. prove queue deduplication against all durable evidence;
8. recheck remote HEAD before publication and reconcile mutable state if it advanced;
9. stop for Main ChatGPT review; no Step05/06/provider calls.
