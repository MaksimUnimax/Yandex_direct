# KW-002 / BLOOD & SAND — STEP08 PRE-ACQUISITION FULL-VOLUME RECONCILIATION — CHATGPT WORK EXECUTION PROMPT

WORK_ID: `KW002_STEP08_PRE_ACQUISITION_RECONCILIATION_2026-09-18`

CONTINUE THE EXISTING KW-002 BLOOD & SAND GREENFIELD SEMANTIC-CORE REHEARSAL.

THIS IS AN EXECUTION TASK.

THIS IS STEP08 PRE-ACQUISITION PREPARATION ONLY.

THIS IS NOT A NEW PROJECT.
THIS IS NOT A METHODOLOGY AUDIT.
THIS IS NOT A FRESH WEB-RESEARCH TASK.
THIS IS NOT WORDSTAT EXECUTION.
THIS IS NOT STEP09.
THIS IS NOT FINAL INTENT.
THIS IS NOT FINAL CLUSTERING.
THIS IS NOT QUERY→PAGE OWNERSHIP.
THIS IS NOT SITE ARCHITECTURE.

Repository:

`MaksimUnimax/Yandex_direct`

Branch:

`roadmap/kwork-productization-2026-08-28`

Job root:

`extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH/work/BLOOD_SAND_GREENFIELD_2026-09-08`

# ======================================================================
# 0. FIRST ACTION — NARROW TECHNICAL PREFLIGHT
# ======================================================================

Fetch/read the CURRENT LIVE remote branch.

Do NOT perform Main Chat's governance cycle.

Do NOT reread all Level1 rules.
Do NOT redo external methodology research.
Do NOT decide whether the roadmap should contain Step08.

Main Chat has already completed those gates and released this bounded contract.

Read and obey exactly:

- `STEP_08_PRE_HANDOFF_MANIFEST_2026-09-18.json`
- `STEP_08_PREPARATION_OUTPUT_SCHEMA_CONTRACT_2026-09-18.json`
- `STEP_08_PREPARATION_METHOD_AND_EXECUTION_GATE_2026-09-18.md`
- `STEP_08_PRE_STEP_EXTERNAL_RESEARCH_AND_SOURCE_TRACE_2026-09-18.md`
- the current release record for this prompt.

Verify every frozen Git blob/tree identity from the pre-handoff manifest.

The preparation base was:

`9933b714e09ecf22b5ff5891add036ea980c142d`

The live HEAD will be later because Main Chat published Step08 preparation/release controls after that base.

That is expected.

If any frozen DATA INPUT blob/tree changed materially:

STOP with:

`AUTHORITY_DRIFT`

and identify the exact file plus expected/actual identity.

Do not continue from a stale data authority.

# ======================================================================
# 1. CURRENT ACCEPTED STATE
# ======================================================================

The current accepted project state is:

```text
STEP07 = PASS_ACCEPTED_WITH_EXTERNAL_SOURCE_LIMITATION
STEP07_RANKING_QUERY_LANE = SOURCE_UNAVAILABLE_DECLARED_LIMITATION
KNOWN_RECALL_LIMITATION = ACTIVE
STEP08 = PREPARATION
STEP08_PROVIDER_EXECUTION = NOT_STARTED
```

Current accepted counts:

```text
STEP07_CANDIDATE_IDENTITIES = 2172
STEP07_PAGE_PROVENANCE_ROWS = 3948
STEP08_ELIGIBLE = 794
  NEW_CANDIDATE = 787
  POSSIBLE_VARIANT = 7
RANKING_QUERY_OBSERVATIONS = 0
```

The ranking-query limitation remains explicit.

Do not try to repair it in this Work run.
Do not search for Keys.so/SpyWords/Topvisor or substitutes.
Do not invent ranking-query evidence.

# ======================================================================
# 2. FULL-VOLUME RULE
# ======================================================================

```text
LARGE DATA != SAMPLE IT
LARGE DATA != TRUNCATE IT
LARGE DATA != FIRST N
LARGE DATA != SUMMARIZE BEFORE ANALYSIS

PROCESS THE COMPLETE BOUNDED EXECUTION UNIT
```

The bounded execution unit is exactly:

`794/794 Step07 candidates`

selected from:

`COMPETITOR_GAP_CANDIDATES.csv`

where:

```text
step08_route = ELIGIBLE_NEW_CANDIDATE
OR
step08_route = ELIGIBLE_POSSIBLE_VARIANT
```

Hard gate:

```text
ELIGIBLE_INPUT_ROWS = 794
ELIGIBLE_INPUT_ACCOUNTED = 794/794
SILENT_SKIP = 0
DUPLICATE_CANDIDATE_ID = 0
```

If the current frozen candidate file does not produce exactly 794 eligible IDs:

STOP with `AUTHORITY_DRIFT_OR_INPUT_CONTRADICTION`.

# ======================================================================
# 3. PURPOSE
# ======================================================================

Step08 does NOT mean:

`one competitor candidate = one Wordstat request`.

The purpose of this preparation is to determine, for all 794 candidates:

1. what exact demand question remains open;
2. whether existing durable KW-002 evidence already answers that same question;
3. whether the candidate adds any genuinely new information need;
4. if new evidence is needed, what bounded Wordstat seed should test it;
5. whether multiple candidates can legitimately share one acquisition seed;
6. what operator/depth/outcome/persistence contract that seed requires.

The target result is the SMALLEST COMPLETE provider-ready seed set justified by evidence.

```text
794 CANDIDATES
-> FULL RECONCILIATION
-> REUSE EXISTING EVIDENCE WHERE VALID
-> COLLAPSE ACQUISITION QUESTIONS ONLY WHERE SEMANTICALLY SAFE
-> PROVIDER-READY SEED QUEUE
```

Do not optimize for few requests at the cost of losing distinct information questions.

Do not optimize for many requests at the cost of duplicate acquisition.

# ======================================================================
# 4. MANDATORY INPUT AUTHORITY
# ======================================================================

Use the exact frozen inputs from:

`STEP_08_PRE_HANDOFF_MANIFEST_2026-09-18.json`

Core authorities include:

- Step07 candidates;
- Step07 candidate provenance;
- Step03A normalized unique pool;
- Step03A normalization/provider lineage;
- corrected Step03B sanitized candidate pool;
- corrected Step03B HOLD/EXCLUDE register;
- Step04 current identity/task signal authority for preliminary context only;
- Step04 targeted expansion queue;
- Step05 existing-evidence reuse register;
- Step05 provider candidate/receipt evidence;
- Step03 Wordstat receipts;
- Step03 and Step05 durable RAW directories where a referenced evidence locator must be verified.

IMPORTANT:

Accepted derived authorities may be used for efficient full-volume joins.

You do NOT need to decompress/read every raw file blindly if an accepted current derived authority plus durable locator already proves the needed lineage.

But when a candidate is closed as `REUSE_EXISTING_EVIDENCE`, the evidence must be materially traceable to durable provider evidence and the scope match must be demonstrable.

If derived evidence is insufficient to prove scope, inspect the referenced RAW/receipt.

# ======================================================================
# 5. SOURCE BOUNDARY
# ======================================================================

Allowed:

- current frozen KW-002 job authorities listed in the manifest;
- current-job files explicitly referenced by those authorities to resolve evidence lineage;
- schema/header information required to parse them.

Forbidden:

- prior Blood & Sand SEO research outside this clean KW-002 job;
- old semantic cores;
- old opportunity maps;
- old competitor conclusions;
- old page/IA decisions;
- fresh external web research;
- commercial competitor-keyword databases;
- Wordstat provider calls;
- Yandex Search provider calls;
- AI Search;
- GenSearch.

```text
WORDSTAT_CALLS = 0
YMB_SEARCH_CALLS = 0
AI_SEARCH_CALLS = 0
GENSEARCH_CALLS = 0
```

# ======================================================================
# 6. RECONCILIATION ORDER — 794/794
# ======================================================================

For every eligible candidate, execute the following order.

## 6.1 Preserve Step07 truth

Carry forward without mutation:

- `candidate_id`;
- displayed/normalized candidate;
- candidate type;
- Step07 reconciliation status;
- Step07 route;
- provenance/source counts;
- immutable raw page evidence/provenance.

Do not rewrite Step07 raw wording.

## 6.2 Define the open demand question

Write a specific evidence question.

Bad:

`Does this keyword have demand?`

Better:

`Does this distinct competitor-derived product/topic formulation expose current Russia-wide Yandex physical-product demand vocabulary not already represented in accepted KW-002 evidence?`

Questions may differ by candidate type.

The question must make clear what existing evidence would count as sufficient.

## 6.3 Exact identity / accepted-universe reconciliation

Check the candidate against current accepted Step03A/Step03B identities.

Exact match can establish existing semantic coverage.

But:

`EXACT TEXT MATCH != AUTOMATICALLY SAME EVIDENCE QUESTION`

and:

`SEMANTIC SIMILARITY != EXACT IDENTITY`.

Do not merge uncertain morphology, synonymy or referents destructively.

## 6.4 Existing provider-evidence scope reconciliation

Search current durable evidence for a seed/request whose tested scope materially answers the open question.

Classify:

```text
EXACT_QUESTION_ANSWERED
QUALIFIED_QUESTION_ANSWERED
RELATED_BUT_INSUFFICIENT
NO_DURABLE_EVIDENCE_FOUND
NOT_APPLICABLE
```

A reuse decision requires:

- durable evidence locator;
- seed/request identity where applicable;
- tested phrase scope;
- provider state;
- explanation why the old evidence answers this question;
- claim boundary.

Hard rule:

```text
BROAD EVIDENCE != QUALIFIED QUESTION ANSWER
RELATED EVIDENCE != CLOSURE
```

Example pattern from prior accepted KW-002 work:

broad `Аум` evidence did not answer the narrower physical-product-qualified `Аум` question.

Apply the principle, not this one example as a lexical special case.

## 6.5 Preparation state

Every candidate gets exactly one:

```text
REUSE_EXISTING_EVIDENCE
ALREADY_COVERED_NO_NEW_INFORMATION_GAIN
PROVIDER_REQUIRED_RECALL_FIRST
PROVIDER_REQUIRED_PRECISION_VALIDATION
HOLD_AMBIGUOUS_OR_SCOPE
STEP07_REWORK_REQUIRED
```

Use `STEP07_REWORK_REQUIRED` only when current accepted evidence demonstrates that the Step07 eligible state itself is materially wrong.

Do not silently rewrite Step07 to make Step08 cleaner.

## 6.6 Acquisition-question grouping

For candidates requiring provider evidence, create acquisition groups.

A group is NOT an SEO cluster.

It means only:

> one Wordstat seed can answer one defined information question for all named member candidates.

Grouping is allowed only when:

- the same provider question truly covers all members;
- no distinct referent is erased;
- no ambiguity is hidden;
- later candidate-level reconciliation remains possible.

Forbidden:

- grouping merely to save money;
- one huge generic seed that cannot answer member-specific questions;
- morphology/synonym collapse without evidence;
- mixing unrelated product/entity meanings.

Every group must retain:

`member_candidate_ids`.

# ======================================================================
# 7. PROVIDER SEED DESIGN — DESIGN ONLY, NO EXECUTION
# ======================================================================

For every unique provider-required acquisition group, materialize one row in:

`STEP08_PROVIDER_SEED_MANIFEST.csv`

No row is authorized for execution.

Always:

`provider_execution_released = false`

## 7.1 Region/device

Current frozen job scope:

```text
REGION = RUSSIA
YANDEX_REGION_ID = 225
DEVICES = DEVICE_ALL
```

Do not change these without evidence from current accepted job authority.

## 7.2 Research mode

Classify:

```text
DISCOVERY_RECALL_FIRST
PRECISION_VALIDATION
EXACT_FORM_TEST
COLLISION_DIAGNOSTIC
```

## 7.3 Operator design

The frozen Main Chat research confirms current Wordstat operators.

Do not redo that web research.

Choose operators from the information question.

No automatic `!`.
No automatic quotes.
No automatic brackets.
No automatic OR grouping merely to reduce request count.

If grouped OR is proposed, explain why the grouped information question is semantically coherent.

## 7.4 Depth design

Current GetTop supports:

`numPhrases = 1..2000`.

Do not treat 2000 as semantic completeness.

For recall-first discovery, 2000 is normally the leading candidate because it maximizes returned observable vocabulary within one request and current billing is per request rather than per returned phrase.

Still record:

- candidate depth options considered;
- selected proposed depth;
- why shallower/deeper options were rejected;
- boundary interpretation.

If 2000 is selected:

```text
RESULTS_ROWS == 2000
-> DEPTH_BOUNDARY_REACHED = true
-> SEMANTIC_UNIVERSE_COMPLETE = false
```

## 7.5 Cost

Frozen current official tariff for planning:

`GetTop = 20 RUB / 1000 requests incl. VAT`.

Use:

`0.02 RUB`

as the current estimated direct provider cost per proposed GetTop request.

This is planning only.

Do not use price to decide relevance.

## 7.6 Current quota and execution chunks

Frozen current official Wordstat quota:

```text
10 statistics requests / second
100 statistics requests / hour
```

Current YMB batch protocol accepts up to 500 phrases/job.

Assign deterministic execution chunks for planning.

Do not drop valid seeds to fit a quota.

The later execution release may further subdivide the queue.

# ======================================================================
# 8. OUTCOME CONTRACT
# ======================================================================

Every provider seed must define all outcomes before execution.

Use:

```text
SUCCESS_WITH_ROWS
SUCCESS_WITH_ZERO_ROWS
SUCCESS_BUT_EVIDENCE_INCOMPLETE
VALIDATION_FAILURE
PROVIDER_FAILURE
OUTCOME_UNKNOWN
```

Required semantics:

### SUCCESS_WITH_ROWS

Bounded positive provider evidence only after complete RAW persistence/readback.

Then future processing:

`RAW -> Step03A-compatible normalization -> Step03B-compatible sanitation -> Step08 reconciliation`.

### SUCCESS_WITH_ZERO_ROWS

Bounded zero observation for that exact:

`phrase/operator/region/device/current snapshot`.

Never universal zero-demand proof.

### SUCCESS_BUT_EVIDENCE_INCOMPLETE

No semantic closure.

### VALIDATION_FAILURE

No semantic answer.

### PROVIDER_FAILURE

No semantic answer.

### OUTCOME_UNKNOWN

No semantic answer plus duplicate-execution risk.

Blind retry forbidden.

Invariant:

`NO_RETRY != NEGATIVE_EVIDENCE`.

# ======================================================================
# 9. OUTPUT FILE 1 — 794-ROW RECONCILIATION
# ======================================================================

Create exactly:

`STEP08_CANDIDATE_PRE_ACQUISITION_RECONCILIATION.csv`

Use the schema exactly from:

`STEP_08_PREPARATION_OUTPUT_SCHEMA_CONTRACT_2026-09-18.json`.

Requirements:

```text
ROWS = 794
DISTINCT candidate_id = 794
INPUT ELIGIBLE candidate_id SET MATCH = EXACT
SILENT_SKIP = 0
```

Sort deterministically by `candidate_id` unless the schema/released manifest provides a stricter sort.

# ======================================================================
# 10. OUTPUT FILE 2 — EXISTING EVIDENCE REUSE
# ======================================================================

Create:

`STEP08_EXISTING_EVIDENCE_REUSE_REGISTER.csv`

Include every durable evidence link materially used to close or inform a candidate question.

One candidate may have multiple evidence rows.

Do not invent provider observations from derived semantic labels.

A reuse row must have an actual durable locator.

# ======================================================================
# 11. OUTPUT FILE 3 — PROVIDER SEED MANIFEST
# ======================================================================

Create:

`STEP08_PROVIDER_SEED_MANIFEST.csv`

This contains only unique justified future provider seeds.

Provider seed count is DERIVED.

It may be much smaller than 794.

Do not target a preferred number.

No minimum provider-call count exists.

If full reconciliation proves zero new calls are needed, zero is a valid analytical result only if every candidate is accounted for and the evidence really supports that result.

If many calls are needed, preserve them; do not truncate for convenience.

# ======================================================================
# 12. OUTPUT FILE 4 — KNOWN FAILURE REGRESSIONS
# ======================================================================

Create:

`STEP08_PRE_ACQUISITION_KNOWN_FAILURE_REGRESSION_MATRIX.csv`

At minimum test applicable mechanisms for:

- provider-success vs evidence persistence;
- destructive normalization;
- unsafe lexical equivalence;
- ambiguity preservation;
- duplicate acquisition;
- owner fact vs demand question;
- operator-choice justification;
- broad-vs-qualified evidence mismatch;
- temporal snapshot overclaim;
- stale provider fact;
- raw provider row promotion;
- next call before RAW readback;
- information-gain contract;
- zero-result overclaim;
- Work sampling/truncation;
- Step07 ranking-source recall limitation visibility.

# ======================================================================
# 13. OUTPUT FILE 5 — QA
# ======================================================================

Create:

`STEP08_PRE_ACQUISITION_QA.md`

Report at least:

```text
WORK_START_REMOTE_HEAD
WORK_PRE_HANDOFF_REMOTE_HEAD
AUTHORITY_DRIFT_STATUS

STEP08_ELIGIBLE_INPUT = 794
STEP08_ELIGIBLE_ACCOUNTED = 794/794
SILENT_SKIP = 0
DUPLICATE_CANDIDATE_ID = 0

REUSE_EXISTING_EVIDENCE = n
ALREADY_COVERED_NO_NEW_INFORMATION_GAIN = n
PROVIDER_REQUIRED_RECALL_FIRST = n
PROVIDER_REQUIRED_PRECISION_VALIDATION = n
HOLD_AMBIGUOUS_OR_SCOPE = n
STEP07_REWORK_REQUIRED = n

REUSE_REGISTER_ROWS = n
PROVIDER_SEEDS = n
PROVIDER_SEED_MEMBER_LINKS = n
UNMAPPED_PROVIDER_REQUIRED_CANDIDATES = 0

DEPTH_DISTRIBUTION
RESEARCH_MODE_DISTRIBUTION
OPERATOR_SHAPE_DISTRIBUTION
ESTIMATED_TOTAL_GETTOP_COST_RUB
PLANNED_EXECUTION_CHUNKS

STEP07_RAW_MUTATIONS = 0
REUSE_ROWS_WITHOUT_DURABLE_LOCATOR = 0
BROAD_EVIDENCE_USED_TO_CLOSE_NARROWER_QUESTION = 0
PROVIDER_SEEDS_WITHOUT_INFORMATION_GAIN = 0
PROVIDER_SEEDS_WITHOUT_OUTCOME_CONTRACT = 0
PROVIDER_SEEDS_WITHOUT_DEPTH_RATIONALE = 0
PROVIDER_SEEDS_WITHOUT_OPERATOR_RATIONALE = 0

WORDSTAT_CALLS = 0
YMB_SEARCH_CALLS = 0
AI_SEARCH_CALLS = 0
GENSEARCH_CALLS = 0
STEP08_PROVIDER_EXECUTION_STARTED = false
```

Also report candidate-state counts by:

- candidate_type;
- Step07 status;
- competitor source-count band;
- evidence reuse class;
- acquisition group size.

Call out concentration where one acquisition seed represents many candidate IDs.

That is a diagnostic, not an automatic failure.

# ======================================================================
# 14. QUALITY SCORE
# ======================================================================

Score the preparation result on the current ten KW-002 dimensions.

Each dimension is 0..10, NOT 0..1.

Report:

```text
1. GOAL_AND_OUTPUT_COMPLETENESS
2. METHOD_AND_SOURCE_SUPPORT
3. INPUT_EVIDENCE_AND_PROVENANCE_INTEGRITY
4. COVERAGE_AND_COMPLETENESS
5. ANALYTICAL_CORRECTNESS_AND_CLAIM_BOUNDARIES
6. ADVERSARIAL_QA_QUALITY
7. PERSISTENCE_READBACK_AND_REPRODUCIBILITY
8. OWNER_CLIENT_USABILITY_AND_PLAIN_LANGUAGE
9. INFORMATION_GAIN_COST_AND_EXECUTION_EFFICIENCY
10. DOWNSTREAM_READINESS

QUALITY_TOTAL = n/100
QUALITY_SCORE = n/10
```

A score cannot override a hard-gate failure.

# ======================================================================
# 15. OUTPUT FILE 6 — HANDOFF MANIFEST
# ======================================================================

Create:

`STEP08_PREPARATION_HANDOFF_MANIFEST.json`

It must include:

- Work ID;
- start/pre-handoff remote HEAD;
- frozen input identities;
- output filenames;
- row counts;
- SHA-256 for every output;
- state counts;
- provider seed count;
- total estimated future provider cost;
- no-provider-call declaration;
- owner staging target;
- transport ZIP SHA-256;
- publication policy;
- Main Chat acceptance = PENDING.

# ======================================================================
# 16. PUBLICATION / OWNER RELAY
# ======================================================================

DO NOT commit/push the Work output directly to canonical final paths.

Package the six required files into ONE transport ZIP.

ZIP is transport only.

Owner uploads all six extracted files together to:

`extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH/work/BLOOD_SAND_GREENFIELD_2026-09-08/_work_staging/STEP08_PRE_ACQUISITION_RECONCILIATION_2026-09-18`

Owner does not route individual files.

Main Chat will perform:

```text
STAGING READBACK
-> FILE/HASH/ROW QA
-> FINAL PLACEMENT
-> STAGING CLEANUP
-> REMOTE READBACK
-> PREPARATION ACCEPTANCE
-> FINAL PROVIDER QUEUE REVIEW
-> ONLY THEN POSSIBLE WORDSTAT EXECUTION RELEASE
```

# ======================================================================
# 17. STOP BOUNDARY
# ======================================================================

STOP after:

- full 794/794 reconciliation;
- existing-evidence reuse register;
- provider-ready seed manifest;
- known-failure regression matrix;
- complete QA;
- handoff manifest;
- transport ZIP.

DO NOT issue any Wordstat command.

DO NOT start any YMB batch.

DO NOT call Search.

DO NOT start Step09.

DO NOT make final intent, cluster, page, URL, H1 or Title decisions.

Final Work status must be one of:

```text
STEP08_PRE_ACQUISITION_RECONCILIATION_COMPLETE
STEP08_PRE_ACQUISITION_REWORK_REQUIRED
AUTHORITY_DRIFT
```

Main Chat owns final acceptance.
