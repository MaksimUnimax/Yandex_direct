# KW-002 / BLOOD & SAND — STEP07 BROWSER RECOVERY — CODEX VERIFIED 80-URL RETRY

Status: **CURRENT / EXECUTION-ONLY / EXACT FROZEN RETRY UNIVERSE**

CONTINUE THE EXISTING KW-002 BLOOD & SAND GREENFIELD SEMANTIC-CORE REHEARSAL.

THIS IS NOT A NEW PROJECT.
THIS IS NOT STEP08.
THIS IS NOT SEMANTIC CANDIDATE CLASSIFICATION.
THIS IS NOT A FULL 1976-URL RESTART.

Your task is to execute real browser navigation for the exact 80 rows frozen by Main Chat, record auditable per-URL outcomes, merge those results into the five corrected browser-recovery artifacts, and return the package for acceptance.

Do NOT redo Main Chat governance/research/release work.
Do NOT make Wordstat, Yandex Search, AI Search or GenSearch calls.
Do NOT classify Step07 semantic candidates.
Do NOT commit/push/PR.

Repository:
`MaksimUnimax/Yandex_direct`

Branch:
`roadmap/kwork-productization-2026-08-28`

Job root:
`extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH/work/BLOOD_SAND_GREENFIELD_2026-09-08`

======================================================================
1. STARTUP
======================================================================

Fetch the CURRENT branch.

Read IN FULL:

```text
STEP07_BROWSER_RECOVERY_FINAL_NARROW_RETRY_MAIN_CHAT_RETURN_QA_2026-09-17.md
STEP07_BROWSER_RECOVERY_CODEX_VERIFIED_80_URL_RETRY_PROMPT_2026-09-17.md
STEP07_BROWSER_RECOVERY_VERIFIED_RETRY_SET_2026-09-17.csv
STEP07_OPERA_BROWSER_CONTROL_PROBE_2026-09-17.md
STEP_07_AUTHORIZED_COMPETITOR_UNIVERSE.csv
```

Use your latest five-file browser-recovery package in the local Codex workspace as the base.

Record:

`CODEX_VERIFIED_RETRY_START_REMOTE_HEAD`

If competitor authority changed materially, STOP with `AUTHORITY_DRIFT`.

Otherwise execute immediately.

======================================================================
2. EXACT RETRY UNIVERSE — DO NOT INFER IT YOURSELF
======================================================================

The only retry universe is:

`STEP07_BROWSER_RECOVERY_VERIFIED_RETRY_SET_2026-09-17.csv`

Expected exact distribution:

```text
A_OLD_T_GOTO_DERIVED = 47
B_EVIDENCE_CAPTURE_UNAVAILABLE = 30
C_UNSUPPORTED_TARGET_BLOCK = 1
D_SOKOLOV_CHALLENGE = 2
TOTAL_UNIQUE_RETRY_ROWS = 80
```

You must process all 80 rows.

Do not replace this with a query over your old errors.
Do not skip a row because you think its previous result is sufficient.
Do not batch-relabel rows without actual browser navigation.

======================================================================
3. REAL BROWSER ACTION REQUIRED FOR EVERY ROW
======================================================================

For each exact retry-set row:

1. start from its exact `source_url_raw`;
2. execute a real browser navigation action using the working browser/computer capability;
3. wait for a meaningful rendered target/browser outcome;
4. record the actual final URL and page title if available;
5. if readable content loads, collect the real rendered page evidence and content SHA-256;
6. if the target itself blocks/challenges, record the exact displayed target evidence;
7. if the execution environment fails first, record the exact actual browser/tool error;
8. write the actual per-row retry timestamp;
9. update that URL row and downstream coverage/evidence deterministically.

Forbidden substitute:

```text
editing error text without navigating
copying one generic error across many rows
using one synthetic timestamp for a batch
claiming RETRIED based only on rewritten CSV data
```

======================================================================
4. REQUIRED RETRY AUDIT ARTIFACT
======================================================================

Create:

`STEP07_BROWSER_RECOVERY_RETRY_AUDIT.csv`

Exactly 80 rows, one per frozen retry ID.

Required schema:

```text
retry_set
recovery_url_id
competitor_authority_id
canonical_site
source_url_raw
prior_terminal_status
prior_error_class
retry_started_at_utc
retry_finished_at_utc
browser_navigation_attempted
browser_route
navigation_result
final_url
page_title
final_terminal_status
final_error_class
exact_final_error_detail
page_evidence_id
content_sha256_if_inspected
notes
```

Hard rules:

```text
RETRY_AUDIT_ROWS = 80
UNIQUE_RETRY_AUDIT_IDS = 80
EVERY_FROZEN_RETRY_ID_PRESENT = true
EXTRA_RETRY_IDS = 0
browser_navigation_attempted = true FOR ALL 80
browser_route = true FOR ALL 80
retry_started_at_utc != blank FOR ALL 80
retry_finished_at_utc != blank FOR ALL 80
```

If final status is inspected, `page_evidence_id` and content SHA must resolve to the corrected JSONL evidence.

If final status is target block, `exact_final_error_detail` must preserve the actual displayed target block/challenge text or equivalent direct observation.

If final status is execution-environment failure, `exact_final_error_detail` must be the actual retry/tool error from that row.

======================================================================
5. KNOWN BROWSER-READABLE REGRESSION CONTROLS
======================================================================

Main Chat independently verified through Opera that these exact retry-set URLs were readable after the prior return:

```text
https://slavyanskieoberegi.ru/katalog-slavyanskix-oberegov/
https://happywitch.ru/catalog/products/amulety_i_talismany/
https://www.oum.ru/yoga/mantry/chto-oznachaet-simvol-om/
```

Your retry must attempt those exact URLs.

If your environment still fails on them, retain the exact actual current failure in the audit. Do not reuse the old generic narrative.

======================================================================
6. WILDBERRIES / TARGET BLOCK CONTROL
======================================================================

The 23 Wildberries rows in retry-set B require actual navigation.

Main Chat observed a browser-level Wildberries message:

`Возможно, нужно выключить VPN`

No bypass is allowed.

For every Wildberries URL:

- if that target message/challenge is actually shown, classify as target anti-bot/block and preserve exact evidence;
- if normal page content loads, inspect it;
- if Codex/browser fails before meaningful target response, keep environment failure with exact actual tool error.

Do not leave them as `EVIDENCE_CAPTURE_UNAVAILABLE` without actual retry evidence.

======================================================================
7. LIVEMASTER / JOOM / KARTASLOV / SOKOLOV
======================================================================

Retry all seven Livemaster/Joom rows in set B normally.

Retry the single Kartaslov row in set C normally. Preserve target challenge only if actually observed.

Retry both Sokolov rows in set D normally. If challenge page persists, classify target challenge with exact displayed evidence. Do not label challenge content as a normal redirect.

======================================================================
8. PRESERVE ALL NON-RETRY DATA
======================================================================

Do not restart or rewrite valid rows outside the frozen 80-row set except deterministic count/coverage consequences of changed retry outcomes.

Expected unchanged universe includes existing valid:

```text
EXCLUDED_OUT_OF_SCOPE
EXCLUDED_DUPLICATE
RECOVERED_INSPECTED
REDIRECTED_IN_SCOPE
page evidence
```

outside the retry set.

======================================================================
9. REGENERATE FIVE CANONICAL RECOVERY FILES
======================================================================

Regenerate/update:

1. `STEP07_BROWSER_RECOVERY_URL_LEDGER.csv`
2. `STEP07_BROWSER_RECOVERY_PAGE_EVIDENCE.jsonl`
3. `STEP07_BROWSER_RECOVERY_COVERAGE.csv`
4. `STEP07_BROWSER_RECOVERY_QA.md`
5. `STEP07_BROWSER_RECOVERY_HANDOFF_MANIFEST.json`

Keep the already accepted exact schemas.

Plus the required sixth control artifact:

6. `STEP07_BROWSER_RECOVERY_RETRY_AUDIT.csv`

======================================================================
10. DATA-DERIVED QA ONLY
======================================================================

Regenerate QA from final files programmatically.

Required hard checks:

```text
SOURCE_URL_ROWS = 1976
AUTHORIZED_COMPETITORS = 32
SILENT_SKIP = 0
URL_LEDGER_SCHEMA = PASS
COVERAGE_SCHEMA = PASS
PAGE_EVIDENCE_SCHEMA = PASS
URL_PK_UNIQUE = PASS
EVIDENCE_PK_UNIQUE = PASS
JOINS = PASS
SHA_VERIFICATION = PASS

FROZEN_RETRY_SET_ROWS = 80
RETRY_AUDIT_ROWS = 80
RETRY_AUDIT_UNIQUE_IDS = 80
FROZEN_RETRY_IDS_MISSING_FROM_AUDIT = 0
EXTRA_RETRY_IDS_IN_AUDIT = 0
ACTUAL_BROWSER_NAVIGATION_ATTEMPTED = 80/80

SET_A_ROWS = 47
SET_A_ACTUAL_BROWSER_RETRIES = 47/47
SET_B_ROWS = 30
SET_B_ACTUAL_BROWSER_RETRIES = 30/30
SET_C_ROWS = 1
SET_C_ACTUAL_BROWSER_RETRIES = 1/1
SET_D_ROWS = 2
SET_D_ACTUAL_BROWSER_RETRIES = 2/2

OLD_GENERIC_NAVIGATION_PLACEHOLDER_ROWS_FINAL = 0
EVIDENCE_CAPTURE_UNAVAILABLE_WITHOUT_NEW_RETRY_AUDIT = 0
UNSUPPORTED_TARGET_BLOCK_ROWS = 0
SOKOLOV_CHALLENGE_MISCLASSIFIED_AS_REDIRECT = 0

FINAL_EXECUTION_ENVIRONMENT_FAILURE_TOTAL = derived from URL ledger
FINAL_ERROR_CLASS_COUNTS = derived from URL ledger
FINAL_ENV_ERROR_CLASS_COUNTS_SUM = FINAL_EXECUTION_ENVIRONMENT_FAILURE_TOTAL

NOT_COLLECTED_ON_INSPECTED = 0
UNAUTHORIZED_DOMAIN_ROWS = 0
UNAUTHORIZED_SITEMAP_REFERENCES = 0
PROVIDER_CALLS = 0
STEP08_EXECUTED = false
CANDIDATE_CLASSIFICATION_EXECUTED = false
GIT_MUTATION = none
```

Do not include target-block classes in the environment-error-class table.

======================================================================
11. MANIFEST
======================================================================

The handoff now contains six files.

Manifest `files[]` must include all six.

For the manifest self-entry:

```text
sha256 = SELF_HASH_NOT_APPLICABLE
byte_size = ACTUAL FINAL MANIFEST BYTE SIZE
row_count_or_jsonl_record_count = 1
```

All other SHA-256 / byte sizes / record counts must match actual final bytes.

======================================================================
12. ZIP / HANDOFF
======================================================================

Create one ZIP containing exactly these six files:

```text
STEP07_BROWSER_RECOVERY_URL_LEDGER.csv
STEP07_BROWSER_RECOVERY_PAGE_EVIDENCE.jsonl
STEP07_BROWSER_RECOVERY_COVERAGE.csv
STEP07_BROWSER_RECOVERY_QA.md
STEP07_BROWSER_RECOVERY_HANDOFF_MANIFEST.json
STEP07_BROWSER_RECOVERY_RETRY_AUDIT.csv
```

Do not commit/push/PR.

Return direct links for six files + one transport ZIP.

Owner returns all six to Main Chat for independent acceptance.

======================================================================
13. PRE-HANDOFF REMOTE CHECK
======================================================================

Fetch current branch again.

Record:

`CODEX_VERIFIED_RETRY_PRE_HANDOFF_REMOTE_HEAD`

If the 32-authority universe materially changed, STOP with `AUTHORITY_DRIFT`.

======================================================================
14. FINAL REPORT
======================================================================

Return at minimum:

```text
CODEX_VERIFIED_RETRY_START_REMOTE_HEAD
CODEX_VERIFIED_RETRY_PRE_HANDOFF_REMOTE_HEAD
FROZEN_RETRY_SET_ROWS = 80
RETRY_AUDIT_ROWS = 80
ACTUAL_BROWSER_NAVIGATION_ATTEMPTED = 80/80
SET_A_ACTUAL_BROWSER_RETRIES = 47/47
SET_B_ACTUAL_BROWSER_RETRIES = 30/30
SET_C_ACTUAL_BROWSER_RETRIES = 1/1
SET_D_ACTUAL_BROWSER_RETRIES = 2/2
FINAL_TERMINAL_STATUS_COUNTS
FINAL_EXECUTION_ENVIRONMENT_FAILURE_TOTAL
FINAL_ERROR_CLASS_COUNTS
UNSUPPORTED_TARGET_BLOCK_ROWS
SOKOLOV_CHALLENGE_MISCLASSIFIED_AS_REDIRECT
MANIFEST_SELF_BYTE_SIZE_MATCH
URL_LEDGER_SCHEMA
COVERAGE_SCHEMA
PAGE_EVIDENCE_SCHEMA
SHA_VERIFICATION
JOIN_QA
PROVIDER_CALLS = 0
STEP08_EXECUTED = false
CANDIDATE_CLASSIFICATION_EXECUTED = false
GIT_MUTATION = none
```

Do not declare PASS unless all 80 exact retry IDs have auditable actual browser actions.