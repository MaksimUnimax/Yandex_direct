# KW-002 / BLOOD & SAND — ACTUAL STEP07 WORK EXECUTION PROMPT

Relay this complete file to ChatGPT Work for a later, separate execution.

```text
PROMPT_STATUS = PREPARED
PROMPT_PURPOSE = ACTUAL_STEP07_ONLY
PREPARATION_BASE_HEAD = 0630d3f6dbd1962290dc8ab77a454e86c604a795
STEP07_EXECUTED_DURING_PREPARATION = false
```

---

CONTINUE THE EXISTING KW-002 BLOOD & SAND GREENFIELD SEMANTIC-CORE REHEARSAL.

THIS IS AN EXECUTION TASK.

THIS IS NOT A NEW PROJECT.

THIS IS ACTUAL STEP07: COMPETITOR SEMANTIC EXPANSION.

THIS IS NOT STEP07_PREPARATION.

THIS IS NOT STEP08.

DO NOT RUN WORDSTAT, YANDEX SEARCH, AI SEARCH OR GENSEARCH.

DO NOT PERFORM FINAL INTENT, FINAL CLUSTERING OR PAGE ARCHITECTURE.

Apply:

```text
LARGE DATA != SAMPLE IT != TRUNCATE IT != SUMMARIZE BEFORE ANALYSIS
LARGE DATA -> PROCESS THE COMPLETE BOUNDED EXECUTION UNIT
```

# ======================================================================
# 0. REPOSITORY / BRANCH / JOB
# ======================================================================

Repository:

`MaksimUnimax/Yandex_direct`

Branch:

`roadmap/kwork-productization-2026-08-28`

Project root:

`extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH`

Job root:

`extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH/work/BLOOD_SAND_GREENFIELD_2026-09-08`

FIRST ACTION:

Fetch/read the current live remote branch. Do not assume preparation HEAD
`0630d3f6dbd1962290dc8ab77a454e86c604a795` is still current. Record exact
live HEAD. If authority-relevant drift exists, stop actual extraction and
reconcile/re-prepare under:

`LEVEL1/WORK_BASE_FRESHNESS_AND_AUTHORITY_DRIFT_RULE.md`

Do not silently use a stale whitelist or input hash.

# ======================================================================
# 1. CURRENT CURSOR AND STOP BOUNDARY
# ======================================================================

Expected entry state after owner publication/readback acceptance:

```text
STEP00 = COMPLETE
STEP01 = COMPLETE
STEP02 = COMPLETE
STEP03 = COMPLETE
STEP03A = COMPLETE
STEP03B = COMPLETE
STEP04 = ACCEPTED
STEP05 = COMPLETE / PASS
STEP06 = DURABLE PASS
STEP07_PREPARATION = ACCEPTED
STEP07 = NOT STARTED
STEP08–STEP20 = NOT STARTED
```

If `STEP07_PREPARATION` has not received owner upload + remote readback + Main
Chat acceptance, do not execute Step07.

This task ends after Step07 candidate discovery, reconciliation, QA and local
handoff packaging. It does not start Step08.

# ======================================================================
# 2. READ AUTHORITY BEFORE EXECUTION
# ======================================================================

Read current live versions of all applicable Level1 rules, `STEP_RULES_INDEX`,
`JOB_FLOW`, current execution cursor, and in particular:

- `LEVEL2/STEP_07_COMPETITOR_SEMANTIC_EXPANSION.md`;
- `STEP_07_PRE_HANDOFF_MANIFEST.md`;
- `STEP_07_OUTPUT_SCHEMA_CONTRACT.json`;
- `STEP_07_AUTHORIZED_COMPETITOR_UNIVERSE.csv`;
- `STEP_07_PREPARATION_EXTERNAL_METHODOLOGY_AUDIT.md`;
- `STEP_07_PREPARATION_QA.md`;
- accepted Step03B/04/05/06 files named by the pre-handoff manifest.

The Step07 Level2 rule and machine-readable schema contract are mandatory.
Do not improvise replacement schemas or statuses.

# ======================================================================
# 3. PURPOSE
# ======================================================================

Step07 uses only Step06-proven search competitors to discover potentially
missing semantic directions for later demand validation.

May discover:

- terminology;
- categories and subcategories;
- product or service names/forms;
- use-cases and audience/task formulations;
- product/use attributes;
- naming, spelling and transliteration variants;
- problem/service formulations;
- informational questions, glossary terms and relevant branches.

Hard claims:

```text
COMPETITOR PAGE TOPIC != PROVEN SEARCH DEMAND
COMPETITOR-DERIVED CANDIDATE != PRODUCTION KEYWORD
BUSINESS RIVAL != SEARCH COMPETITOR
FAMILY != FINAL INTENT != SERP CLUSTER != PAGE
STEP07 DISCOVERS CANDIDATES
STEP08 VALIDATES DEMAND
```

# ======================================================================
# 4. EXACT COMPETITOR AUTHORITY
# ======================================================================

Use exactly:

`STEP_07_AUTHORIZED_COMPETITOR_UNIVERSE.csv`

Expected frozen preparation state:

```text
AUTHORITY_ROWS = 32
AUTHORITY_IDS = S07A001..S07A032
UNIQUE_AUTHORITY_IDS = 32
UNIQUE_CANONICAL_SITES = 32
SOURCE_STEP06_REGISTRY_SHA256 = b15e601db56d8f3c23d7a8c4a2193fc14000dc9773229d693c200756240efcdb
```

Validate every row against current:

`KW002_STEP06_SEARCH_COMPETITOR_REGISTRY_HARDENED.csv`

Do not add:

- any of the other raw Step06 recurrence domains;
- a business competitor named by the client or analyst;
- an external SEO-tool competitor;
- a sibling/parent/subdomain not explicitly authorized;
- a domain found during competitor-page traversal.

External links may be recorded as out-of-scope discoveries but may not become
new competitor authorities.

# ======================================================================
# 5. EXACT UPSTREAM RECONCILIATION AUTHORITY
# ======================================================================

Use current accepted files listed and hashed in
`STEP_07_PRE_HANDOFF_MANIFEST.md`.

Required layers:

```text
Step03A occurrences = 25,979
Step03A normalized identities = 24,576
Step03B KEEP = 5,100
Step03B HOLD = 13,035
Step03B EXCLUDE = 6,441
Step04 family rows = 32
Step04 identity rows = 24,576
Step04 occurrence rows = 25,979
Step04 queue rows = 13
Step05 queue rows = 13
Step05 new provider candidate rows = 1
Step05 new union phrase rows = 0
Step06 classified SERP rows = 440
Step06 competitor registry rows = 32
```

These are expectations for drift detection, not permission to override live
accepted replacements. If current accepted authority differs, stop and
reconcile; do not silently reshape Step07.

Do not use superseded/uncorrected Step03B or historical Step04 outputs.

# ======================================================================
# 6. PUBLIC SOURCE SURFACES
# ======================================================================

Allowed on each exact authorized host, within its `scope_policy`:

- Step06-observed source URLs;
- category/subcategory pages;
- product/service pages;
- public taxonomy, menus and breadcrumbs;
- collection/use-case/landing pages;
- public articles, guides, FAQ/help and glossaries;
- titles, headings, labels and concise contextual text;
- public robots/sitemap files for URL discovery;
- public structured page metadata attributable to the page;
- sequential pagination required for complete eligible enumeration.

Forbidden:

- login/private areas;
- CAPTCHA, robots, paywall or anti-bot bypass;
- private/hidden APIs;
- unbounded external-domain traversal;
- site-search submissions used as a substitute for discoverable taxonomy;
- scraping user data or unrelated user-generated content;
- new search-demand provider calls.

# ======================================================================
# 7. HOST-SCOPE POLICY
# ======================================================================

Obey each authority row:

### `EVIDENCE_ANCHORED_RELEVANT_SUBTREE`

For broad platforms/information sites, begin with every Step06-observed URL and
follow public taxonomy/navigation only inside the same relevant theme/branch.
Do not crawl the full marketplace, encyclopedia or dictionary.

### `THEME_SCOPED_PUBLIC_TAXONOMY`

For specialized/thematic sites, enumerate all public category/product/
informational URLs in the relevant theme. Exclude unrelated branches with an
explicit reason.

No arbitrary top-N, “important pages” sample or representative subset.

# ======================================================================
# 8. URL INVENTORY / FRONTIER
# ======================================================================

Create `STEP07_SOURCE_URL_LEDGER.csv` before candidate aggregation.

Discovery sources, in order:

1. all Step06-observed URLs for the authority host;
2. public navigation/breadcrumb/category links inside scope;
3. public robots-listed sitemaps and scoped sitemap indexes;
4. sequential pagination inside eligible collections;
5. in-scope redirect targets.

For every discovered URL retain:

- raw URL;
- computed canonical URL;
- declared canonical URL if visible;
- redirect target;
- discovery source and parent;
- page type;
- scope status;
- robots/access/inspection status;
- candidate occurrence count;
- extraction time and failure/exclusion reason.

Use the exact enums in `STEP_07_OUTPUT_SCHEMA_CONTRACT.json`.

Canonicalization:

- resolve relative URLs;
- lowercase scheme/host only;
- consistently map Unicode/IDNA host identity;
- remove fragments for retrieval identity;
- normalize standard ports and dot segments;
- remove only enumerated tracking/session parameters;
- preserve content-changing parameters and product variants;
- retain meaningful pagination;
- reject duplicate sort/filter/facet expansions under explicit rules;
- do not cross to an unauthorized host;
- retain canonical conflicts as ambiguity.

# ======================================================================
# 9. ACCESS / INACCESSIBLE EVIDENCE
# ======================================================================

Respect robots and ordinary public access. Do not bypass.

Record explicit terminal states for:

```text
robots restriction
authentication/login
CAPTCHA/anti-bot
HTTP failure
timeout/network failure
deleted/not found
redirect in/out of scope
dynamic/infinite-scroll unresolved
technical error
```

Inaccessible is not “no candidate”. Unresolved dynamic coverage is not
inspected coverage. Fabricated page content is forbidden.

# ======================================================================
# 10. FULL-VOLUME COVERAGE AND STOPPING CONDITION
# ======================================================================

Maintain exactly one `STEP07_COMPETITOR_COVERAGE_LEDGER.csv` row for every
authorized competitor.

For each authority prove:

```text
discovered_urls
eligible_urls
inspected_urls
excluded_urls
inaccessible_urls
redirected_terminal_urls
candidate_yield_urls
no_candidate_urls
unresolved_urls
candidate_identity_count
terminal_coverage_status
```

Reconcile:

```text
DISCOVERED_URLS
= INSPECTED_URLS
 + EXCLUDED_URLS
 + INACCESSIBLE_URLS
 + REDIRECTED_TERMINAL_URLS
 + UNRESOLVED_URLS
```

The frontier stops only when no discovered eligible URL remains unprocessed.
If scale exceeds one run, use deterministic full chunks (sitemap file, stable
path prefix or canonical URL sort range). Do not sample. Keep Step07
`INCOMPLETE` until all chunks close.

# ======================================================================
# 11. CANDIDATE EXTRACTION
# ======================================================================

Create one provenance row per candidate occurrence in:

`STEP07_CANDIDATE_PROVENANCE_LEDGER.csv`

Eligible evidence locations include:

```text
TITLE
H1
H2_H6
BREADCRUMB
NAVIGATION
CATEGORY_LABEL
PRODUCT_NAME
SERVICE_NAME
USE_CASE_LABEL
ATTRIBUTE_LABEL
FAQ_QUESTION
GLOSSARY_TERM
BODY_TEXT
STRUCTURED_METADATA
```

Every provenance row must identify the exact source URL, page type, section,
reproducible location/selector, raw wording, concise source context, normalized
candidate and transformation.

Do not destroy or overwrite raw wording after normalization.

# ======================================================================
# 12. NORMALIZATION
# ======================================================================

Apply in order:

1. retain exact `raw_wording` and `source_context`;
2. decode valid HTML entities/remove markup;
3. Unicode NFC;
4. trim outer/collapse internal whitespace;
5. casefold only for `normalized_comparison_key`;
6. remove UI/formatting noise only under an enumerated rule;
7. preserve material punctuation, digits, hyphens and language distinctions;
8. preserve competitor brand/product/category wording;
9. never use automatic lemmatization/synonym collapse as exact deduplication;
10. record every transformation.

Brand removal or long-form shortening may create a separate derived candidate
only with explicit transformation provenance. The raw branded/long occurrence
remains.

Historical demand evidence for phrase A must not be assigned to transformed
phrase B.

# ======================================================================
# 13. RECONCILIATION TAXONOMY
# ======================================================================

Assign exactly one:

```text
ALREADY_PRESENT
NEW_CANDIDATE
NORMALIZED_DUPLICATE
POSSIBLE_VARIANT
OUT_OF_SCOPE
AMBIGUOUS
```

Rules:

- `ALREADY_PRESENT`: exact upstream-compatible comparison key resolves to an
  accepted identity; record layer/ID; audit-only.
- `NEW_CANDIDATE`: no exact accepted identity; in scope; not a duplicate; may
  route to Step08 as unvalidated.
- `NORMALIZED_DUPLICATE`: same permitted comparison key; link to canonical
  candidate; keep all provenance; no extra Step08 row.
- `POSSIBLE_VARIANT`: morphological, lexical, transliteration, spelling or
  likely synonym relation; keep separate; may route separately to Step08.
- `OUT_OF_SCOPE`: explicit enumerated reason; audit-only.
- `AMBIGUOUS`: unresolved meaning/referent/transformation; HOLD before Step08.

Reconcile in this order:

```text
STEP03A exact identity
-> STEP03B corrected state
-> STEP04 preliminary context/ambiguity
-> STEP05 closure/context
-> Step07 status and Step08 route
```

Step04 family similarity alone is not an exact upstream match.

# ======================================================================
# 14. DEDUPLICATION
# ======================================================================

- identical raw wording: exact duplicate occurrence;
- identical allowed comparison key: normalized duplicate;
- same candidate across several URLs/domains: one candidate summary plus all
  provenance rows;
- morphology/lexical/transliteration/spelling/likely synonym: keep separate as
  `POSSIBLE_VARIANT` unless exact accepted key exists;
- distinct intent-like wording: keep separate pending later SERP analysis.

```text
LOOKS SIMILAR != DUPLICATE
DEDUPLICATION != FINAL CLUSTERING
```

# ======================================================================
# 15. REQUIRED OUTPUTS
# ======================================================================

Create exactly these primary actual-Step07 outputs in the job root:

1. `COMPETITOR_GAP_CANDIDATES.csv`
2. `STEP07_COMPETITOR_COVERAGE_LEDGER.csv`
3. `STEP07_SOURCE_URL_LEDGER.csv`
4. `STEP07_CANDIDATE_PROVENANCE_LEDGER.csv`
5. `STEP07_EXECUTION_QA.md`

Use exact schemas, enums, keys, sort order and relationships from:

`STEP_07_OUTPUT_SCHEMA_CONTRACT.json`

Do not create final target pages, clusters, URLs, H1s or Titles.

# ======================================================================
# 16. HARD QA
# ======================================================================

Programmatically verify at least:

1. only Step06-authorized competitors used;
2. all 32 frozen authorized competitors accounted for, unless drift forced a
   re-preparation before execution;
3. all eligible discovered surfaces accounted for;
4. inaccessible/blocked surfaces explicitly recorded;
5. no arbitrary sample/top-N used;
6. every candidate has one or more provenance rows;
7. every raw wording preserved;
8. every transformation recorded;
9. reconciliation against accepted Step03B/04/05 layers completed;
10. no duplicate candidate occurrence silently discarded;
11. multi-source provenance and exact source counts preserved;
12. no competitor topic treated as proven demand;
13. Wordstat calls = 0;
14. Yandex Search calls = 0;
15. AI Search/GenSearch calls = 0;
16. final intent decisions = 0;
17. clustering decisions = 0;
18. page/URL/H1/Title decisions = 0;
19. HOLD/AMBIGUOUS/ERROR/inaccessible/unresolved counts explicit;
20. all coverage counts reconcile;
21. every foreign key resolves;
22. candidate summary counts equal provenance-derived counts;
23. all enums valid and required fields nonblank;
24. deterministic sort order PASS;
25. accepted upstream files unchanged;
26. execution completeness demonstrable.

If any hard QA fails, correct output before packaging. If evidence limitations
make correction impossible, report Step07 `INCOMPLETE`; do not hide the gap.

# ======================================================================
# 17. CLAIM AND SNAPSHOT LIMITS
# ======================================================================

The result proves only what was observed on legitimately accessible public
competitor surfaces during this bounded execution snapshot.

Do not claim:

- the competitor site will remain unchanged;
- the URL inventory is permanent;
- an inaccessible surface has no relevant language;
- competitor terminology has market demand;
- repeated competitor language is a final keyword/intent/cluster/page.

# ======================================================================
# 18. PUBLICATION / HANDOFF
# ======================================================================

Do not commit, push, force-push, open a PR or publish to GitHub.

After local QA:

1. freeze all final actual-Step07 files;
2. create one ZIP containing only the final handoff files with repository
   relative paths;
3. provide direct downloadable links for every file and the ZIP;
4. list exact target paths and NEW/REPLACE status;
5. provide owner upload targets and intended commit message;
6. set owner upload/readback/Main Chat acceptance to pending.

# ======================================================================
# 19. FINAL REPORT
# ======================================================================

Return:

1. exact live HEAD used;
2. Step07 execution status;
3. QA PASS/FAIL per hard invariant;
4. files created;
5. exact row counts and reconciliation totals;
6. 32-authority coverage status counts;
7. candidate reconciliation totals;
8. inaccessible/unresolved/error totals;
9. direct file links and one ZIP link;
10. exact target paths and commit message;
11. unresolved items;
12. explicit confirmations:

```text
NEW_WORDSTAT_CALLS = 0
NEW_YANDEX_SEARCH_CALLS = 0
NEW_AI_SEARCH_OR_GENSEARCH_CALLS = 0
STEP08_STARTED = false
FINAL_INTENT_DECISIONS = NONE
FINAL_CLUSTER_DECISIONS = NONE
FINAL_PAGE_DECISIONS = NONE
OWNER_UPLOAD_COMPLETE = false
REMOTE_READBACK_PASS = false
MAIN_CHAT_ACCEPTANCE = PENDING
```

Do not stop at discovery, a sample, a plan or partial ledgers. Actual Step07 is
complete only when the bounded full-volume execution and QA are complete.
