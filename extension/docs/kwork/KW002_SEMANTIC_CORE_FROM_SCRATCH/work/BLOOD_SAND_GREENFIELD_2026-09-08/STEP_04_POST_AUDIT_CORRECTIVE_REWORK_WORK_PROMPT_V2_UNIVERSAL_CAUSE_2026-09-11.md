# KW-002 — W08 Step04 post-audit full-volume correction, universal-cause version

Date: 2026-09-11
Status: **CANONICAL EXECUTION PROMPT V2 / FULL-VOLUME RULE-SYSTEM CORRECTION / STEP05 BLOCKED**

Continue the existing KW-002 job in the current repository/branch.

THIS IS NOT A NEW PROJECT.
THIS IS NOT STEP05.
THIS IS NOT STEP06.
THIS IS NOT A PROVIDER-ACQUISITION TASK.
THIS IS A FULL-VOLUME CORRECTION OF STEP04 AFTER AN ACCEPTED INDEPENDENT AUDIT.

## 0. Core interpretation

The current job exposed concrete bad rows/families. Those are **regression evidence only**.

Do NOT implement permanent or current correction logic as special cases for:

- one product/site vocabulary;
- one family ID;
- one exact bad phrase;
- one current row count.

Correct the universal underlying mechanisms described by Level1/Level2.

## 1. Fetch current live branch first

Fetch the current live branch and record the actual base HEAD.

Do not reset, force-push or overwrite unrelated concurrent work.

## 2. Read universal authorities IN FULL

### Level1

- `LEVEL1/ROADMAP_AND_METHOD_GENERALIZATION_RULE.md`
- `LEVEL1/EXECUTION_FAILURE_LEDGER_AND_ANTI_REGRESSION_RULE.md`
- `LEVEL1/DATA_VOLUME_SANITATION_AND_DELIVERY_SCOPE_RULE.md`
- `LEVEL1/JOB_DATA_SEPARATION_AND_LIFECYCLE.md`
- `LEVEL1/PRE_STEP_EXTERNAL_RESEARCH_AND_SOURCE_DISCLOSURE_RULE.md`
- `LEVEL1/WORK_HANDOFF_RULE.md`

### Level2

- `LEVEL2/README.md`
- `LEVEL2/STEP_RULES_INDEX.md`
- `LEVEL2/STEP_04_PRELIMINARY_FAMILY_TRIAGE_QUALITY_GATE.md`

The universal rules define WHY and HOW to correct. Job-root audit files define WHERE the defects appeared in this execution.

## 3. Read job-specific authorities IN FULL

Job root:
`extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH/work/BLOOD_SAND_GREENFIELD_2026-09-08`

Read:

- current job failure ledger;
- Step04 audit failure addendum;
- Main ChatGPT W07 review;
- W07 audit report/overlay/family/boundary/queue-feedback/metrics/regression matrix;
- accepted corrected Step03A/Step03B authorities;
- superseded post-sanitation Step04 result/materializer/QA.

Do not use sealed prior research.

## 4. Frozen boundaries

```text
STEP03A_MUTATION_ALLOWED = false
STEP03B_MUTATION_ALLOWED = false
WORDSTAT_CALLS_ALLOWED = 0
ORDINARY_SEARCH_CALLS_ALLOWED = 0
GENSEARCH_CALLS_ALLOWED = 0
AI_SEARCH_CALLS_ALLOWED = 0
STEP05_ADVANCEMENT_ALLOWED = false
STEP06_ADVANCEMENT_ALLOWED = false
FINAL_INTENT_CLASSIFICATION_ALLOWED = false
SERP_CLUSTERING_ALLOWED = false
QUERY_TO_PAGE_ALLOWED = false
SITE_IA_ALLOWED = false
KNOWN_ROW_PATCH_ONLY_ALLOWED = false
JOB_SPECIFIC_SPECIAL_CASE_RULES_ALLOWED = false
```

## 5. Universal root causes that MUST be corrected

### U1 — unsafe lexical shortcut / prefix-stem overreach

Production rules must not use an unbounded prefix/stem as conclusive semantic proof when sibling lexemes can collide.

Required behavior:

```text
bounded token/morphology/context evidence
+ explicit collision tests
+ ambiguity preservation when unresolved
```

Current job's known toy/game examples are regression fixtures only.

### U2 — first-match precedence hides a stronger explicit task

Broad topic/context must not erase a more informative user action/task/object.

Required behavior:

```text
collect simultaneous material signals where practical
OR
use an explicit specificity/precedence contract
```

Preserve secondary topic/context as metadata rather than discarding it.

### U3 — generic fallback hides an unmodelled coherent task

A generic family is not proof that the query lacks a user task.

Required behavior:

```text
independent hidden-task discovery
+ large generic-family heterogeneity checks
+ extensible preliminary task representation
```

If a repeated coherent task exists, represent it at least as a preliminary task marker/family without inferring final page/intent.

### U4 — hand-built taxonomy validates itself

Do not accept corrected family logic using only the corrected classifier's own assertions.

Required behavior:

Use an independent diagnostic not equivalent to the production routing, such as simultaneous task signals plus term/co-occurrence/topic/semantic boundary analysis.

Diagnostic output is not final SERP clustering.

### U5 — local coverage gap ignores global durable evidence

Before any Step05 queue row can be provider-ready:

```text
reconcile against ALL durable prior evidence
→ prove incremental information gain
→ prove negative-result value
→ define stop condition
```

Owner/business facts must not be sent to a search provider as if demand evidence can prove inventory/service/claim truth.

## 6. Full-volume rerun

Run the corrected Step04 rule system from scratch over the complete current normalized universe.

Current job accounting must remain reconciled to the accepted upstream authorities.

Do NOT edit only the identities listed by W07.

For every normalized identity:

- preserve current Step03B state/reason unchanged;
- recompute Step04 preliminary family/task assignment under corrected general rules;
- preserve secondary signals/context where useful;
- materialize old→new transition;
- record deterministic assignment reason;
- map all RAW occurrences to the corrected normalized assignment;
- preserve EXCLUDE history outside active Step04 semantic scope.

Family count/taxonomy is emergent. Do not target the old family count.

## 7. Required current-job regression fixtures

The accepted W07 concrete defects must all disappear as manifestations of the generalized fixes:

- no non-game sibling lexeme may be routed as GAME solely by the old broad prefix mechanism;
- no “unqualified broad-topic” family may retain rows with stronger explicit task signals that the corrected precedence contract should preserve;
- no generic “unqualified product/service” family may absorb the known coherent make/craft task class merely because that class was absent from the old taxonomy;
- no provider-ready expansion row may duplicate equivalent durable evidence.

Re-evaluate the complete blast radius; report all sibling changes beyond known fixtures.

## 8. Preliminary-method boundary

Corrected Step04 remains preliminary triage only.

Every family authority must distinguish at least:

```text
family/topic context
primary user-task hypothesis
business relevance confidence
family coherence confidence
ambiguity class
coverage state
later evidence needed
final_intent_status = NOT_FINAL_AT_STEP04
serp_cluster_status = NOT_EVALUATED_AT_STEP04
```

Do not convert Step04 families into pages or final SEO clusters.

## 9. Correct expansion queue without executing Step05

Rebuild/reconcile the queue from the corrected family authority.

Each row must state:

```text
unresolved question
current durable evidence already available
OWNER_FACT vs SEARCH_DEMAND distinction
incremental information gain
negative-result value
stop condition
provider readiness state
```

Generic allowed queue states include:

```text
VALID_INCREMENTAL_SEARCH_GAP
OWNER_FACT_FIRST
EXISTING_EVIDENCE_REUSE
DUPLICATE_OR_LOW_INFORMATION_PROBE
DEFER_TO_LATER_INTENT_OR_SERP
```

No provider calls.

## 10. Correct sanitation feedback

Rebuild feedback from corrected Step04 findings.

Rules:

- do not mutate Step03B;
- distinguish upstream sanitation defect from Step04 family-routing defect;
- add missing systematic feedback classes discovered by independent audit;
- remove/repair feedback classes that were artifacts of defective family routing.

## 11. Independent post-correction audit

Mandatory independent second diagnostic over the complete active/HOLD universe.

At minimum test:

```text
UNBOUNDED_PREFIX_USED_AS_SEMANTIC_PROOF = 0
KNOWN_LEXEME_PREFIX_COLLISIONS_UNTESTED = 0
EXPLICIT_TASK_HIDDEN_BY_BROADER_TOPIC = 0
RULE_PRECEDENCE_CONTRACT = PRESENT
REPEATED_EXPLICIT_TASK_PATTERN_HIDDEN_IN_GENERIC = 0
LARGE_GENERIC_FAMILY_HETEROGENEITY_AUDIT = PASS
INDEPENDENT_FAMILY_COHERENCE_DIAGNOSTIC = PASS
PRELIMINARY_FAMILY_USED_AS_FINAL_PAGE_CLUSTER = 0
PROVIDER_READY_QUEUE_WITH_EQUIVALENT_EXISTING_EVIDENCE = 0
OWNER_FACT_GATED_SENT_TO_PROVIDER = 0
RAW_LINEAGE_LOSS = 0
STEP03B_STATE_MUTATIONS = 0
FREQUENCY_USED_AS_SEMANTIC_ASSIGNMENT_PROOF = 0
```

Also rerun all older applicable semantic collision regressions from the universal failure ledger and current job history.

## 12. Required outputs

Materialize at minimum:

1. corrected occurrence→family ledger;
2. corrected family triage authority;
3. corrected targeted expansion queue;
4. corrected sanitation feedback register;
5. old→new correction transition ledger;
6. corrected known-failure regression matrix;
7. independent corrected family/coherence QA authority;
8. corrected QA report;
9. Work return;
10. artifact manifest with sizes/hashes;
11. deterministic corrected materializer/source.

Use the established job-specific filenames from the earlier W08 prompt unless a schema/version suffix is required to avoid overwrite ambiguity.

Do not erase historical Step04/W07 artifacts.

## 13. Acceptance gate

PASS_CANDIDATE requires ALL:

```text
FULL_VOLUME_ACCOUNTING = PASS
RAW_LINEAGE_LOSS = 0
STEP03B_STATE_MUTATIONS = 0
UNIVERSAL_ROOT_CAUSE_FIXES = PASS
ALL_APPLICABLE_OLD_REGRESSIONS = PASS
ALL_W07_JOB_FIXTURES = PASS
INDEPENDENT_POST_CORRECTION_DIAGNOSTIC = PASS
OPEN_CRITICAL_RULE_DEFECTS = 0
QUEUE_PROVIDER_READY_DUPLICATE_EVIDENCE = 0
OWNER_FACT_GATED_SENT_TO_PROVIDER = 0
SEALED_SOURCE_VIOLATIONS = 0
PROVIDER_CALLS = 0
STEP05_ADVANCEMENT = false
STEP06_ADVANCEMENT = false
```

Do not inherit any old score. Compute a fresh score only from the corrected result.

## 14. Publication

Persist complete artifacts in the job root.

Use native Git if already authenticated; otherwise use approved owner relay with exact files/ZIP + direct GitHub upload target. ZIP is transport-only unless the repository explicitly expects the archive itself.

Remote readback is mandatory after publication.

## 15. Stop

STOP after corrected Step04 artifacts, full-volume QA, publication/owner-relay preparation and Work return.

Return to Main ChatGPT for independent acceptance.

Do NOT resume Step05 in the same pass.
