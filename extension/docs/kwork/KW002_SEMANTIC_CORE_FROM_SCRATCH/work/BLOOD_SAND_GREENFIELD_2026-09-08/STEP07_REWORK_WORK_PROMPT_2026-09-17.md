# KW-002 / BLOOD & SAND — STEP07 FULL-VOLUME REWORK WORK PROMPT

Status: **CURRENT / EXECUTION-ONLY / REWORK OF FAILED STEP07 ATTEMPT**

CONTINUE THE EXISTING KW-002 BLOOD & SAND GREENFIELD SEMANTIC-CORE REHEARSAL.

THIS IS AN EXECUTION TASK.
THIS IS NOT A NEW PROJECT.
THIS IS NOT STEP07 PREPARATION.
THIS IS NOT STEP08.
THIS IS A FULL-VOLUME CORRECTIVE REWORK OF ACTUAL STEP07 AFTER MAIN CHAT RETURN QA REJECTED ATTEMPT 1.

Do NOT redo Main Chat governance, external methodology research, owner-facing reporting or release authorization.

Do NOT run Wordstat, Yandex Search, AI Search or GenSearch.
Do NOT make final intent, clustering, query→page, site architecture, URL, H1 or Title decisions.

Repository:
`MaksimUnimax/Yandex_direct`

Branch:
`roadmap/kwork-productization-2026-08-28`

Job root:
`extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH/work/BLOOD_SAND_GREENFIELD_2026-09-08`

======================================================================
1. NARROW STARTUP PREFLIGHT ONLY
======================================================================

Fetch the CURRENT remote branch and record `WORK_START_REMOTE_HEAD`.

Verify current existence/identity of:

```text
STEP07_MAIN_CHAT_RETURN_QA_2026-09-17.md
STEP07_REWORK_WORK_PROMPT_2026-09-17.md
STEP_07_PRE_HANDOFF_MANIFEST.md
STEP_07_OUTPUT_SCHEMA_CONTRACT.json
STEP_07_AUTHORIZED_COMPETITOR_UNIVERSE.csv
KW002_STEP06_SEARCH_COMPETITOR_REGISTRY_HARDENED.csv
COMPETITOR_GAP_CANDIDATES.csv
STEP07_COMPETITOR_COVERAGE_LEDGER.csv
STEP07_SOURCE_URL_LEDGER.csv
STEP07_CANDIDATE_PROVENANCE_LEDGER.csv
STEP07_EXECUTION_QA.md
STEP07_EXECUTION_HANDOFF_MANIFEST.json
```

Verify that the authorized competitor universe remains exactly 32 and still maps to the frozen Step06 curated registry.

If competitor membership, accepted upstream semantic authority, Step07 output schema or claim boundary materially changed after this prompt was released:

```text
STEP07_REWORK = BLOCKED_AUTHORITY_DRIFT
STOP
REPORT EXACT CONFLICT
```

Otherwise EXECUTE IMMEDIATELY.

Do not turn this preflight into a second project-governance cycle.

======================================================================
2. WHY REWORK IS REQUIRED
======================================================================

Attempt 1 is preserved in Git history and is NOT accepted Step07 authority.

Main Chat found two independent blocking defects.

### DEFECT A — incomplete coverage caused materially by execution-route failures

Attempt 1 produced:

```text
DISCOVERED_URLS = 1976
INSPECTED_URLS = 24
EXCLUDED_URLS = 1201
INACCESSIBLE_URLS = 751
```

Only `S07A010 / kartaslov.ru` yielded inspected candidate evidence.

Many inaccessible rows preserve runtime/proxy failures such as:

```text
ClientHttpProxyError:403
url='http://127.0.0.1:...'
```

Such an execution-environment proxy failure is NOT sufficient source-level closure.

Main Chat subsequently confirmed through ordinary legitimate public retrieval that multiple authorized competitor sites recorded by Attempt 1 as zero-inspected/network-inaccessible had publicly retrievable relevant pages on the same date, including:

```text
https://slavyanskieoberegi.ru/
https://slavyanskieoberegi.ru/slavyanskie-oberegi/
https://happywitch.ru/catalog/products/amulety_i_talismany/
https://simvolroda.ru/
https://simvolroda.ru/catalog/ot-bolezney/
```

These URLs are access-route controls only; they do not expand competitor authority beyond the frozen 32.

### DEFECT B — systematic semantic candidate producer contamination

Attempt 1 promoted arbitrary prose / literary quotations / broken dictionary fragments / metadata-like wording into `NEW_CANDIDATE` and routed them toward Step08.

Examples proving the failure mechanism include:

```text
10 схем вышивки крестиком для дизайна детской
[Муров:] Добрые люди обещали мне никогда не снимать с него [сына] медальона.
amuletum, с араб.].
amuletum]
А на груди висел серебряный медальон в виде перевёрнутой пентаграммы.
Антон Платов, «Славянские руны», 2001 г.
```

THESE ARE REGRESSION EXAMPLES, NOT PATCH TARGETS.

Do NOT manually fix only these rows.

Required correction:

```text
FIX / STRENGTHEN THE CANDIDATE-ELIGIBILITY PRODUCER
→ REPROCESS THE COMPLETE AFFECTED EXISTING UNIVERSE
→ THEN MERGE / RECONCILE NEWLY RETRIED COMPETITOR EVIDENCE
```

======================================================================
3. FULL-VOLUME REWORK UNIVERSE
======================================================================

Rework BOTH:

### 3A. Current failed-attempt semantic universe

Process ALL:

```text
686 current candidate identities
1417 current provenance rows
1976 current URL ledger rows
32 current competitor coverage rows
```

Do not take a sample.

Current statuses are NOT trusted merely because mechanical schema QA passed.

Every existing candidate/provenance relation must be re-evaluated under the corrected producer logic.

### 3B. Missing/retry competitor coverage

For all authorized competitors/URLs where Attempt 1 did not obtain legitimate inspected content, retry public access using a legitimate browser-capable route available in ChatGPT Work / cloud browser.

Do not use the failed raw-HTTP/proxy route as the sole retry mechanism when it reproduces local proxy failures before reaching the target.

Do NOT bypass:

- robots restrictions;
- authentication/login;
- CAPTCHA;
- paywalls;
- anti-bot controls.

If a normal browser route reaches a public page, inspect it and continue the allowed bounded frontier under the frozen scope policy.

If the target itself blocks access or a browser-capable legitimate route still cannot retrieve the page, preserve the correct access evidence.

Hard interpretation:

```text
TARGET / ROBOTS / CAPTCHA / LOGIN EVIDENCE
may support source-level inaccessible evidence

WORK RUNTIME / LOCAL PROXY FAILURE BEFORE TARGET RESPONSE
!= SOURCE-LEVEL CLOSURE
```

A pure execution-environment failure must remain incomplete/unresolved for semantic coverage unless another legitimate route resolves it.

======================================================================
4. AUTHORIZED COMPETITOR / SOURCE BOUNDARY
======================================================================

Use exactly the current `STEP_07_AUTHORIZED_COMPETITOR_UNIVERSE.csv`.

Expected unless live authority legitimately changed before rework release:

```text
AUTHORIZED_COMPETITORS = 32
AUTHORITY_IDS = S07A001..S07A032
SOURCE_REGISTRY_SHA256 = b15e601db56d8f3c23d7a8c4a2193fc14000dc9773229d693c200756240efcdb
```

No arbitrary new competitors.
No sibling-domain inheritance.
No old Blood & Sand competitor conclusions.

Follow each row's current scope policy:

```text
EVIDENCE_ANCHORED_RELEVANT_SUBTREE
THEME_SCOPED_PUBLIC_TAXONOMY
```

======================================================================
5. CORRECTED CANDIDATE-ELIGIBILITY PRODUCER
======================================================================

A Step07 candidate identity must be an **independently meaningful semantic direction suitable for later demand validation**, not merely any piece of text containing an in-scope token.

Candidate types remain the frozen schema types:

```text
TERMINOLOGY
CATEGORY
SUBCATEGORY
PRODUCT_NAME
USE_CASE
ATTRIBUTE
NAMING_VARIANT
PROBLEM_FORMULATION
SERVICE_FORMULATION
INFORMATIONAL_FORMULATION
OTHER
UNKNOWN
```

### 5.1 Valid candidate characteristics

A candidate may be retained when the wording itself represents a compact, reproducible semantic object/direction such as:

- a recognized term/name/symbol;
- category/subcategory;
- product/service naming direction;
- audience/use-case formulation;
- material/attribute/form factor;
- spelling/transliteration/naming variant;
- stable problem/service formulation;
- stable informational question/topic formulation.

It must make sense independently of the surrounding source sentence.

### 5.2 Raw prose is provenance, not automatically candidate identity

The following MUST NOT automatically become `NEW_CANDIDATE` merely because a product/amulet/rune-related word appears:

```text
literary quotation / dialogue line
sentence corpus/example sentence
book/author/year citation
bibliographic metadata
broken etymology fragment
truncated punctuation fragment
generic UI text
"вам также может быть интересно"
"о книге"
"примечания"
button/navigation boilerplate without independent topical meaning
incidental object mention inside unrelated prose
unrelated article/product theme
```

Map them to the correct audit state/reason (`OUT_OF_SCOPE`, UI/noise, foreign/unrelated, or `AMBIGUOUS`) unless a valid separately derived candidate exists.

### 5.3 Long body text may produce a derived candidate only under a strict reproducible rule

A long sentence/paragraph may yield a candidate only if:

1. a concise semantic span/formulation is explicitly present or can be derived by a minimal reproducible transformation;
2. the derived wording is independently meaningful;
3. the exact raw source text remains in provenance;
4. `transformation_rule` and detail state exactly how the candidate was derived;
5. no new business/search-demand claim is invented.

Use schema-supported transformation states such as `LONG_FORM_SPAN_DERIVATION` where appropriate.

Do not transform arbitrary prose into a desired keyword merely because it sounds useful.

### 5.4 Informational/dictionary pages are not globally forbidden

Do NOT overcorrect by excluding every dictionary/informational page.

A heading/topic such as a genuine meaning/etymology/synonym/use-case formulation can be a valid informational candidate if it is compact, in scope and independently meaningful.

The defect is **arbitrary text-as-candidate**, not informational content itself.

### 5.5 Claim boundary

Mystical/symbolic/health/protective wording may be evidence of what users/competitors discuss. It is NOT accepted as a true physical/medical/business efficacy claim.

```text
COMPETITOR CLAIM LANGUAGE != CLIENT FACT
COMPETITOR CLAIM LANGUAGE != PROVEN SEARCH DEMAND
```

Keep the semantic wording only when it is a valid candidate direction; do not certify the claim as true.

======================================================================
6. FULL-AFFECTED-UNIVERSE REGRESSION
======================================================================

Known bad examples are regression tests only.

Required:

```text
MANUAL_KNOWN_ROW_PATCH_ONLY = false
ALL_686_EXISTING_CANDIDATE_IDENTITIES_REEVALUATED = true
ALL_1417_EXISTING_PROVENANCE_ROWS_REEVALUATED = true
ALL_NEWLY_COLLECTED_OCCURRENCES_EVALUATED_UNDER_SAME PRODUCER = true
```

Do not preserve current `NEW_CANDIDATE = 400` merely to minimize delta.

Report exact old→new status transition matrix, at minimum:

```text
FROM_STATUS
TO_STATUS
ROW_COUNT
```

Report how many existing candidate IDs are retained, reclassified, superseded or regenerated under deterministic sorting.

No silent row/provenance loss.

======================================================================
7. URL / COVERAGE REWORK
======================================================================

Start from the existing `STEP07_SOURCE_URL_LEDGER.csv` and coverage ledger as failed-attempt evidence.

Preserve discovery lineage and prior failure notes.

For each authorized competitor:

- retry eligible rows that were not inspected due runtime/network access failure;
- expand newly reachable in-scope navigation/taxonomy/sitemap/pagination frontier;
- apply the same canonicalization/dedup rules;
- assign exactly one current terminal state to every current discovered canonical URL;
- preserve prior attempt evidence in notes when a state changes.

Coverage still must reconcile:

```text
DISCOVERED_URLS
= INSPECTED_URLS
+ EXCLUDED_URLS
+ INACCESSIBLE_URLS
+ REDIRECTED_TERMINAL_URLS
+ UNRESOLVED_URLS
```

But semantic completion must not be claimed merely because a runtime proxy failure was assigned a terminal accounting bucket.

For `COMPLETE_WITH_INACCESSIBLE_EVIDENCE`, the remaining inaccessible rows must represent legitimate target/access evidence, not merely an untried alternative after a Work-local proxy failure.

If legitimate public coverage still cannot be completed, Step07 remains `INCOMPLETE` with exact evidence.

======================================================================
8. RECONCILIATION AGAINST UPSTREAM SEMANTIC AUTHORITY
======================================================================

Use the current accepted inputs named by `STEP_07_PRE_HANDOFF_MANIFEST.md`.

Reconcile every valid candidate:

```text
STEP03A exact identity
→ STEP03B corrected state
→ STEP04 preliminary context
→ STEP05 closure/context
→ Step07 status
→ Step08 route
```

Statuses remain exactly:

```text
ALREADY_PRESENT
NEW_CANDIDATE
NORMALIZED_DUPLICATE
POSSIBLE_VARIANT
OUT_OF_SCOPE
AMBIGUOUS
```

Only valid `NEW_CANDIDATE` and eligible `POSSIBLE_VARIANT` may route toward Step08.

Step08 is NOT executed in this task.

======================================================================
9. REQUIRED REWORK OUTPUTS
======================================================================

Produce replacements for the same six canonical files:

```text
COMPETITOR_GAP_CANDIDATES.csv
STEP07_COMPETITOR_COVERAGE_LEDGER.csv
STEP07_SOURCE_URL_LEDGER.csv
STEP07_CANDIDATE_PROVENANCE_LEDGER.csv
STEP07_EXECUTION_QA.md
STEP07_EXECUTION_HANDOFF_MANIFEST.json
```

All six handoff-manifest actions are `REPLACE`.

Git history preserves Attempt 1; do not create alternate production filenames merely to avoid replacing the failed current attempt.

Do NOT include `JOB_FLOW.md` or cursor in Work output. Main Chat updates mutable state after return QA.

======================================================================
10. HARD QA FOR REWORK
======================================================================

In addition to all current Step07 schema/foreign-key/count gates, explicitly test:

```text
ONLY_AUTHORIZED_COMPETITORS = PASS
ALL_32_COMPETITORS_ACCOUNTED = PASS
ARBITRARY_TOP_N_OR_SAMPLE = 0
RUNTIME_PROXY_FAILURE_MISTAKEN_FOR_SOURCE_CLOSURE = 0
ALL_RETRYABLE_RUNTIME_NETWORK_FAILURES_RETRIED_VIA_LEGITIMATE_BROWSER_ROUTE = true
FULL_EXISTING_686_CANDIDATE_UNIVERSE_REEVALUATED = true
FULL_EXISTING_1417_PROVENANCE_UNIVERSE_REEVALUATED = true
KNOWN_BAD_EXAMPLES_NO_LONGER_ROUTE_AS_NEW_CANDIDATE = PASS
GENERIC_UI_NOISE_ROUTED_AS_STEP08_CANDIDATE = 0
BIBLIOGRAPHIC_METADATA_ROUTED_AS_STEP08_CANDIDATE = 0
LITERARY_EXAMPLE_SENTENCE_ROUTED_AS_STEP08_CANDIDATE = 0
BROKEN_TEXT_FRAGMENT_ROUTED_AS_STEP08_CANDIDATE = 0
LONG_FORM_DERIVED_CANDIDATES_HAVE_EXPLICIT_REPRODUCIBLE_TRANSFORMATION = true
RAW_WORDING / SOURCE CONTEXT PRESERVED = true
OLD_TO_NEW_STATUS_TRANSITION_MATRIX_PRESENT = true
SILENT_PROVENANCE_LOSS = 0
STEP08_PROVIDER_CALLS = 0
```

Also include the full existing Step07 hard QA contract and universal ten-dimension score.

The rework may PASS only if:

```text
ALL HARD GATES PASS
QUALITY_TOTAL >= 90/100
QUALITY_SCORE >= 9.0/10
NO OPEN CRITICAL DEFECT
```

If coverage remains legitimately incomplete, return INCOMPLETE truthfully even if semantic classification is corrected.

======================================================================
11. PRE-PACKAGING REMOTE CHECK
======================================================================

Before packaging:

- fetch current remote HEAD;
- record `WORK_PRE_PUBLICATION_REMOTE_HEAD`;
- verify no material Step07 authority/schema/input change occurred;
- if it did, stop with exact authority drift rather than improvising.

======================================================================
12. HANDOFF
======================================================================

Work MUST NOT commit/push/PR.

Use one owner staging target:

```text
repository = MaksimUnimax/Yandex_direct
branch = roadmap/kwork-productization-2026-08-28
directory = extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH/work/BLOOD_SAND_GREENFIELD_2026-09-08
upload URL = https://github.com/MaksimUnimax/Yandex_direct/upload/roadmap/kwork-productization-2026-08-28/extension/docs/kwork/KW002_SEMANTIC_CORE_FROM_SCRATCH/work/BLOOD_SAND_GREENFIELD_2026-09-08
```

Provide:

- direct downloadable link for each of six replacement files;
- one transport-only ZIP containing exactly the six files;
- one final-path manifest;
- one upload link;
- owner instruction: extract, upload all six together, commit, reply `готово`.

Do not ask owner to route files.

======================================================================
13. FINAL WORK REPORT
======================================================================

Return at minimum:

```text
WORK_START_REMOTE_HEAD
WORK_PRE_PUBLICATION_REMOTE_HEAD
AUTHORITY_DRIFT_STATUS
STEP07_REWORK_STATUS
```

And exact old→new accounting for:

- discovered / inspected / excluded / inaccessible / unresolved / error URLs;
- coverage status across all 32 competitors;
- candidate totals by reconciliation status;
- old→new candidate status transition matrix;
- existing provenance rows processed;
- new provenance rows added;
- candidate/source/provenance reconciliation;
- runtime network failures retried/resolved/still unresolved;
- all hard QA gates;
- quality total /100 and average /10.

Explicitly confirm:

```text
NEW_WORDSTAT_CALLS = 0
NEW_YANDEX_SEARCH_CALLS = 0
NEW_AI_SEARCH_OR_GENSEARCH_CALLS = 0
STEP08_STARTED = false
FINAL_INTENT_DECISIONS = NONE
FINAL_CLUSTER_DECISIONS = NONE
FINAL_PAGE_DECISIONS = NONE
WORK_GITHUB_COMMIT_PUSH_PR = false
OWNER_UPLOAD_COMPLETE = false
REMOTE_READBACK_PASS = false
MAIN_CHAT_ACCEPTANCE = PENDING
```

Do not stop at a sample or manual fix of known bad rows.

This rework is complete only after the full affected existing semantic universe and all legitimately retryable missing competitor coverage have been processed under the corrected producer and QA contract.