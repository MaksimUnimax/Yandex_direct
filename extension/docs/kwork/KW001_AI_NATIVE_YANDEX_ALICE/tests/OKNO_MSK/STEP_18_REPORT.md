# OKNO_MSK — STEP 18 PRIORITIZATION REPORT

Date: 2026-09-03  
Step: 18 — Prioritization  
Status: **EXECUTED / ANALYTICAL QA PASS / FINAL GITHUB READBACK PENDING AT WRITE TIME**

## 1. Whole Kwork goal

Deliver an evidence-based Yandex/Search + selective AI-search semantic and site-architecture package that tells the client which existing pages should serve which user/search jobs, what should be changed or expanded, what should remain unchanged, where uncertainty must be preserved, and what should be done first — without promising rankings, traffic, leads, revenue or stable Alice/AI visibility.

## 2. Step 18 goal

Convert the accepted current architecture and evidence from Steps 0–17 into one auditable implementation order that answers:

- what should be done first;
- what can follow later;
- what is only a supporting implementation task;
- what must remain on hold instead of being guessed;
- why each material action has its priority;
- what evidence and limitations govern the recommendation.

Step 18 was not allowed to reopen the accepted architecture merely to manufacture additional work.

## 3. Execution result

The current job now has one canonical priority action register:

`STEP_18_ACTION_REGISTER.tsv`

Priority result:

```text
TOTAL ACTION REGISTER ROWS = 34
P1_HIGH = 12
P2_MEDIUM = 20
P3_LATER = 1
HOLD = 1
```

The single HOLD action represents **20 exact upstream DEFER_PENDING_EVIDENCE structural units**. Those 20 units are individually enumerated in `STEP_18_HOLD_RECHECK_LEDGER.tsv`; none was placed on HOLD merely because demand is low.

No numeric magic score was used. Impact, public business relevance, demand, structural urgency, evidence strength, bounded AI support, dependency and uncertainty remained separate dimensions. Implementation effort remained `UNKNOWN` where the job contains no real implementation estimate.

## 4. P1 — what should be addressed first

### 4.1 Current owner / page-role corrections and overlap checks

Eight P1 actions correct or protect current page responsibility before broad downstream routing:

1. Use the newly discovered standalone balcony-finishing page as the exact owner for standalone finishing rather than preserving the old no-standalone assumption (`D14A005`).
2. Use the dedicated sliding-balcony page for sliding-only intent while keeping the cold-glazing page primary when cold intent is explicit (`D14A006`).
3. Separate the roles of the broad panoramic commercial owner and the newly discovered materially overlapping panoramic page; do **not** merge/delete/redirect from Step18 (`D14A008`).
4. Separate the roles of the private-house primary and the cottage/country-house same-task page; do **not** perform destructive consolidation from Step18 (`D14A009`).
5. Route wooden-house PVC intent to the exact current wooden-house specialist (`D14A011`).
6. Route commercial glass-unit selection to the exact current commercial glass-unit hub while keeping informational selection separate (`D14A012`).
7. Separate the current product-integrated Rehau comparison owner from the newly discovered same-task comparison article; destructive consolidation remains forbidden (`D14A017`).
8. Use the exact sliding-veranda page for explicit sliding intent while retaining `/verandy/` for general non-sliding glazing (`D14A021`).

These actions are P1 because later routing, links or content should not be implemented against a page-responsibility model that already has a known current-site correction or same-task overlap requiring explicit differentiation.

### 4.2 Highest-priority verified content improvements

Four additional P1 actions address accepted current-page gaps with meaningful user/business relevance:

9. French-window commercial page — strengthen verified configuration/replacement and French-vs-panoramic selection depth. Step17 case `C15-013` is merged into this existing action rather than duplicated.
10. Private-house windows planning — add clear standard/non-standard opening dimensions, sizing principles and why actual measurement overrides generic standards.
11. Window-hardware selection guide — strengthen hardware construction/parts and broader manufacturer/brand comparison coverage for the accepted 28-phrase task.
12. PVC-door commercial page — add door-specific professional installation scope/process and price or price-estimation guidance.

These are content improvements to existing accepted owners. They do not authorize new standalone pages.

## 5. P2 — important supporting work

`P2_MEDIUM = 20`.

This tier contains:

- the remaining **13/21 Step14A material current-site deltas** that improve supporting routing without replacing the primary owner;
- aluminium-window technical ventilation/micro-ventilation guidance;
- French-window definition/terminology guidance;
- PVC-door dimensions/selection guidance;
- portfolio categorization/filters for balcony, veranda, panoramic and French examples;
- Step17 `C15-004` bounded panoramic-aluminium content recheck/enhancement candidate;
- Step17 `C15-020` bounded best-windows freshness/comparison-methodology content recheck;
- the batch of **15 already accepted contextual internal links**.

The Step17 AI-derived items remain content/recheck overlays. They do not authorize an AI-only architecture change.

## 6. P3 — later routing implementation

`P3_LATER = 1` action row:

`S18-A033` accounts for all **46 ROUTE_TO_EXISTING_PAGE_AS_SUBTASK** structural units.

This work is valid, but it follows higher-precedence exact owner/boundary corrections and Step14A overlays so broad routing is not implemented using an outdated owner model.

## 7. HOLD — unresolved evidence, not low value

`S18-A034` represents all **20 DEFER_PENDING_EVIDENCE** structural units.

The exact hold ledger contains 20/20 items and a recheck trigger for each. Main blocker classes are:

- unverified current business/product/service truth;
- unresolved DIY/content-policy decision;
- ambiguous or malformed user task;
- missing authoritative normative evidence;
- insufficient evidence for a truthful current owner/content action.

Canonical interpretation:

```text
HOLD != LOW VALUE
HOLD != REJECTION
HOLD = NAMED BLOCKING UNCERTAINTY + RECHECK TRIGGER
```

Examples include unverified mosquito-net repair, aluminium-window replacement, roof windows, window/blind/windowsill repair, DIY content roles and the hardware standards/GOST topic.

## 8. Structural-universe accounting — 168/168

Accepted Step12/Step14 structural distribution:

```text
KEEP_EXISTING_STRUCTURE = 72
ROUTE_TO_EXISTING_PAGE_AS_SUBTASK = 46
DEFER_PENDING_EVIDENCE = 20
NO_STANDALONE_PAGE = 15
OUTSIDE_SCOPE_NO_ACTION = 7
ADD_SECTION_OR_FAQ_TO_EXISTING = 6
EXPAND_EXISTING_PAGE = 2
TOTAL = 168
```

Step18 accounting:

```text
72 KEEP = preserved no-action unless a later accepted overlay creates a distinct action
46 ROUTE = accounted by S18-A033
20 DEFER = accounted by S18-A034 + 20-row hold ledger
15 NO_STANDALONE = preserved no-action
7 OUTSIDE_SCOPE = preserved no-action
6 ADD_SECTION/FAQ = S18-A009, A010, A012, A026, A027, A028
2 EXPAND = S18-A011, A029
TOTAL ACCOUNTED = 168/168
SILENT DROPS = 0
```

The later Step14A actions are overlays on this accepted universe; they do not inflate the base structural-unit count.

## 9. Step14A accounting — 21/21

Every material current-site delta is represented exactly once in the Step18 action register:

```text
D14A001..D14A021 = 21/21 ACCOUNTED
```

Important boundaries preserved:

```text
UNAUTHORIZED NEW PAGE ACTIONS = 0
UNAUTHORIZED MERGE/DELETE/REDIRECT/CANONICAL ACTIONS = 0
```

The three same-task overlap findings (`D14A008`, `D14A009`, `D14A017`) are prioritized as role-differentiation/recheck work, not as automatically destructive consolidation.

## 10. Internal-link accounting — 58/58

Accepted Step14 link state:

```text
TOTAL LINK ROWS = 58
IMPLEMENT = 15
DEFERRED / NOT APPLICABLE = 43
```

Step18 result:

- all 15 accepted IMPLEMENT edges are represented by `S18-A032`;
- all 43 deferred/not-applicable rows remain non-actions;
- silent promotion of a deferred link = 0.

## 11. Step17 AI accounting — 8/8

All selected Step17 cases are explicitly accounted:

| Case | Step17 result | Step18 treatment |
|---|---|---|
| C15-004 | DE_RISK + CONTENT_EXPANSION_CANDIDATE | `S18-A030` bounded content recheck/enhancement |
| C15-006 | DE_RISK + INSUFFICIENT | no content action; insufficient evidence preserved |
| C15-007 | DE_RISK + NO_MATERIAL_CONTENT_GAP_OBSERVED | no new action |
| C15-010 | NO_CHANGE + NO_MATERIAL_CONTENT_GAP_OBSERVED | no new action |
| C15-013 | DE_RISK + CONTENT_EXPANSION_CANDIDATE | merged into existing French content action `S18-A009` |
| C15-018 | NO_CHANGE + INSUFFICIENT | no content action; insufficient evidence preserved |
| C15-019 | NO_CHANGE + NOT_APPLICABLE | no new action |
| C15-020 | INSUFFICIENT + CONTENT_EXPANSION_CANDIDATE | `S18-A031` bounded freshness/methodology content recheck |

Accounting:

```text
STEP17 CASES = 8/8
CONTENT_EXPANSION_CANDIDATES = 3/3
AI-ONLY ARCHITECTURE PROMOTIONS = 0
```

Current AI evidence remains bounded: exact-query diagnostic, not a sitewide or longitudinal Alice visibility study.

## 12. Preserved unresolved phrases — 19/19

The 19 Step14 phrases previously judged not architecture-material under current evidence remain explicitly preserved.

```text
PRESERVED UNRESOLVED = 19/19
IMPLEMENTATION ACTIONS MANUFACTURED FROM THEM = 0
```

This does **not** claim they have zero demand. It means current accepted evidence does not justify changing the architecture because of them.

## 13. Business/private-data boundary

Current base rehearsal has no private Webmaster/Metrika/Direct evidence and several internal acquisition priorities remain unknown.

Therefore Step18 did not fabricate:

- margin or lead value;
- sales/capacity priority;
- conversion/revenue priority;
- paid-performance priority;
- historical traffic-loss/cannibalization harm;
- implementation cost/effort.

Public business relevance was used only where supported by the current public offer/site role and accepted job scope.

## 14. Bridge/provider accounting

Step18 required no new provider evidence.

```text
WORDSTAT CALLS = 0
SEARCH CALLS = 0
GENSEARCH CALLS = 0
WEBMASTER CALLS = 0
METRIKA CALLS = 0
DIRECT CALLS = 0
NEW PAID PROVIDER COST = 0 RUB
UNAUTHORIZED PROVIDER CALLS = 0
```

Persisted Steps 0–17 evidence was sufficient for the active Step18 acceptance gate.

## 15. Adversarial QA result

Checked failure classes:

```text
LOW DEMAND USED AS SOLE DOWNGRADE = 0
EVIDENCE STRENGTH USED AS BUSINESS IMPACT = 0
PRIVATE CLIENT PRIORITY GUESSES = 0
UNKNOWN EFFORT GUESSES = 0
MAGIC NUMERIC SCORE = 0
AI-ONLY ARCHITECTURE PROMOTIONS = 0
UNAUTHORIZED NEW PAGE ACTIONS = 0
UNAUTHORIZED DESTRUCTIVE ACTIONS = 0
DUPLICATE CANONICAL ACTIONS = 0
SILENT STRUCTURAL DROPS = 0
EVERY HOLD HAS RECHECK TRIGGER = true
FORWARD TRACE = PASS
REVERSE TRACE = PASS
```

Analytical Step18 QA therefore passes. Final GitHub readback is performed after all Step18 artifacts/state/flow are written.

## 16. Step verdict and transition

At analytical execution level:

```text
STEP18_EXECUTION = COMPLETE
STEP18_ANALYTICAL_QA = PASS
STEP18_PROVIDER_CALLS = 0
STEP18_PAID_COST_RUB = 0
STEP19_EXECUTION_AUTHORIZED = false
```

Step19 is permanently `UNVALIDATED`; Step18 completion may allow **Step19 pre-step methodology research/review only** after final Step18 GitHub readback succeeds. It does not authorize Step19 execution.

## ПРОСТЫМИ СЛОВАМИ — ИТОГ

**Зачем делали этот шаг:** после всех предыдущих проверок у нас уже было много правильных решений по сайту, но клиенту нужен не склад рекомендаций, а понятная очередь — что исправлять сначала, что потом и где пока нельзя принимать решение без дополнительной информации.

**Что фактически сделали:** собрали все подтверждённые изменения в один список, убрали повторения, сначала поставили исправления ролей страниц и самые важные подтверждённые пробелы в содержании, затем вспомогательные улучшения и ссылки, а спорные темы отдельно оставили на проверку вместо того, чтобы что-то выдумывать. Никаких новых страниц, удалений или слияний без разрешения предыдущих шагов не добавляли.

**Что получили и что это даёт дальше:** получили готовый порядок работ по сайту: 12 действий в первую очередь, 20 следующим уровнем, один большой пакет маршрутизации позже и один блок из 20 нерешённых тем, к которым можно вернуться только при появлении нужных данных. Этот порядок уже можно использовать как основу для клиентского плана работ на следующем этапе после финальной проверки сохранённых файлов.
