# OKNO_MSK — Step18/Step19 correction from Step20 defects

Date: 2026-09-03  
Owner authorization: **RECEIVED — correct D20-001..D20-004**  
Provider calls: **0**  
Step20 rerun: **NOT AUTHORIZED / REQUIRED AFTER THIS CORRECTION**  
Step21: **BLOCKED**

## Why this correction exists

Step20 adversarial Final QA correctly refused handoff and found three MATERIAL defects plus one MINOR hygiene defect. The owner then explicitly authorized correction of the affected Step18/Step19 work.

The correction does not rerun Step20 and does not start Step21.

## D20-001 — TEST/DEMO distribution identity

Frozen `TEST_ORDER.md` defines this job as a mock commercial rehearsal rather than a real paid-client engagement.

Correction applied:

- XLSX README now visibly states `TEST/DEMO CASE` and `mock commercial rehearsal; not an actual paid-client engagement`;
- DOCX contains the same visible disclosure near the title;
- PDF contains the same visible disclosure on page 1;
- `STEP_19_01_CLIENT_SUMMARY.md` and `STEP_19_09_CLIENT_DELIVERY_MESSAGE.md` contain explicit TEST/DEMO disclosure;
- physical manifest now records:

```text
test_demo_case = true
mock_commercial_rehearsal = true
actual_paid_client_engagement = false
```

## D20-002 — S18-A012 current-content scope

Step20 proved that the current PVC/REHAU door page already materially contains price/price-estimation guidance.

Correct current action:

```text
retain existing price/price-estimation guidance
+
strengthen only still-missing door-specific professional installation scope/process
```

The correction explicitly forbids duplicating existing price factors/calculator/measurer guidance.

Current authority is represented by the higher-precedence overlays:

- `STEP_18_STEP20_CURRENT_CONTENT_CORRECTION_OVERLAY.tsv`;
- `STEP_18_STEP20_WORK_PACKAGE_CORRECTION_OVERLAY.tsv`;
- corrected Step19 action/priority views;
- corrected `Execution_Calibration` row `S18-WP-A012`.

Priority remains `P1_HIGH`; this does not create a production schedule.

## D20-003 — S18-A027 current-content scope

Step20 proved that the current French-window page already contains a basic concise definition.

Correct current action:

```text
DO NOT add another basic definition.
If residual terminology ambiguity remains,
fold only French-vs-panoramic / naming distinction guidance into S18-A009.
```

`S18-A027` is now explicitly downstream/combined with `S18-A009`, and the exact package depends on `S18-A009`.

Priority remains `P2_MEDIUM`; no duplicate content task is authorized.

## D20-004 — DOCX metadata hygiene

The rebuilt DOCX now cleans the generic library/template metadata used by the previous package:

- author is blank rather than `python-docx`;
- last modified by is blank;
- generic `generated by python-docx` comments/description is removed;
- created/modified dates are 2026-09-03 rather than inherited 2013 timestamps.

No sensitive personal metadata was introduced.

## Deterministic rebuild and failed-attempt accounting

The correction deliberately did not hide failed build/QA attempts.

### Run 33765153795 — FAILED

Cause: A027 TSV correction row omitted the required `P2_MEDIUM` column, shifting subsequent fields.

Lesson/control:

```text
TSV ROW PRESENT != TSV SCHEMA ALIGNED
```

The overlay was corrected and later verification uses exact field equality rather than a loose text-only check.

### Run 33765468333 — FAILED

Cause: independent QA used a nonexistent python-docx `CoreProperties.creator` property. The actual API field is `author`.

Lesson/control:

```text
ASSUMED LIBRARY PROPERTY NAME != VERIFIED API PROPERTY
```

The correction verifier now checks the real property and the underlying core XML.

### Run 33765760718 — FAILED

Cause: author/timestamps were cleaned, but generic `generated by python-docx` text remained in another core metadata field.

Lesson/control:

```text
ONE METADATA FIELD CLEAN != DISTRIBUTION METADATA HYGIENE COMPLETE
```

The final correction clears author, last-modified-by, comments/description and stale dates, then checks `docProps/core.xml` directly.

### Run 33765988306 — SUCCESS

All stages passed:

```text
materialize Step19 baseline = PASS
apply Step20-derived action/package corrections = PASS
build corrected physical package = PASS
independent correction QA = PASS
persist corrected data/builder/binaries/QA = PASS
upload exact artifact = PASS
```

Workflow artifact:

```text
artifact id = 9897510555
artifact name = step18-step19-step20-defect-corrected-package-v2
artifact digest = sha256:b4a8a0796374321c110d2e7290a8ed84c9ea742edeef72ea2e849b2801679879
```

## Current physical package identity

```text
STEP_19_CLIENT_WORKBOOK_CORRECTED.xlsx
bytes = 458600
sha256 = 9f08cb47b1f4863f90b84c2d3a1ae145341ff5fd5f9c57ee76f2c087c642d499

STEP_19_CLIENT_REPORT_CORRECTED.docx
bytes = 44078
sha256 = d68001ee36f1677cf0817e3058e490bdfcc1598da324b3cd62a5670c1644b3dd

STEP_19_CLIENT_REPORT_CORRECTED.pdf
bytes = 59868
sha256 = cf4a208ba5286243e41ef5dff31a5d3eea9fdc0dab93800850addcc07663915e
```

## Physical QA after exact artifact download

The successful workflow artifact was downloaded and the exact persisted files were checked again locally.

Workbook:

- 9 expected sheets;
- README visibly marked TEST/DEMO;
- 2332 semantic rows;
- 112 execution packages;
- 7 measurement classes;
- corrected A012/A027 action, priority and calibration content present;
- formula-error scan = 0;
- key sheet renders reviewed.

DOCX:

- 6/6 rendered pages visually inspected;
- TEST/DEMO disclosure visible;
- corrected A012 visible;
- no observed clipping/overlap/broken table layout.

PDF:

- 4/4 rendered pages visually inspected;
- TEST/DEMO disclosure visible on page 1;
- corrected A012 visible;
- no observed clipping/overlap/broken layout.

## Accounting preserved

```text
semantic rows = 2332
SEARCH_REQUIRED = 19
actions = 34
execution packages = 112
  exact action = 31
  internal link = 15
  route to existing = 46
  HOLD/recheck = 20
measurement classes = 7
supported new-page actions = 0
supported destructive actions = 0
provider calls = 0
new paid cost = 0 RUB
```

Owner/effort/capacity remain real calibration inputs and are not fabricated. Final sprint/calendar sequence remains `PENDING_CALIBRATION`.

## Workflow consequence

The four Step20 defects have been corrected upstream, but the **old Step20 verdict does not turn into PASS automatically**.

Correct state after this correction:

```text
STEP18 = correction applied / analytical priority valid / schedule still pending calibration
STEP19 = correction applied / physical package rebuilt and QA passed
STEP20 = previous run complete with CORRECTION_REQUIRED; corrected package now requires a fresh rerun
STEP21 = blocked until fresh Step20 rerun passes
```

## Next legal action after correction readback

```text
OWNER_AUTHORIZATION_FOR_STEP20_RERUN_ON_CORRECTED_PACKAGE
```
