# Report №02 — client instruction cleanup after owner review

Status: **ANALYST RECHECK PASS / INDEPENDENT FINAL PDF READBACK PASS / OWNER REVIEW PENDING**

## Owner-identified failure classes corrected

The owner rejected remaining client-facing text that described internal process, things the specialist should not do, or obvious browser/editor mechanics instead of the actual specialist work.

Permanent Report №02 gates now record these failures:

- client process-history narration;
- negative pseudo-actions instead of required work;
- obvious `open/find/navigate/check` filler in specialist steps;
- repeated instructions across fields and rows;
- empty contents/TOC furniture in the physical document;
- field labels orphaned from the content they introduce at a page break.

The report-specific content gate now requires a specialist brief containing required work, concrete clarification tasks and observable acceptance results only. The physical-layout gate separately requires final committed DOCX/PDF inspection.

## Client report cleanup

Removed from Report №02:

- report-stage process narration;
- `не делать` / `не внедрять` / `не размещать` style pseudo-actions;
- readiness meta-language such as `не выдавать за готовую задачу`;
- abstract `получить доказательство` language;
- obvious `откройте / найдите / перейдите / проверьте` mechanics;
- repeated `физически страницу не менять` text across 46 semantic rows;
- repeated identical missing-context text across 14 internal-link rows;
- client-facing prohibition blocks.

Replaced with:

- positive action language;
- action-specific professional operations;
- exact `Что уточнить` tasks for the five clarification items;
- compact semantic mapping table;
- compact internal-link pair table;
- positive preservation requirements;
- one common acceptance section.

## Physical materialization correction

The first clean-language PDF readback exposed two layout defects that were not visible to the text validator:

1. a standalone empty `Содержание` heading generated by the generic TOC path;
2. mini field labels such as `Пример смысловой реализации`, `Что уточнить` and `Результат уточнения` could remain at the bottom of a page while their content continued on the next page.

These were promoted into the permanent physical-layout gate:

`STEP_20_REPORT_02_SPECIALIST_IMPLEMENTATION_GUIDE_LAYOUT_GATE.md`

The Report №02 physical builder now:

- omits the unused TOC for this compact specialist guide;
- keeps mini field labels with the following content;
- renders the final PDF for post-commit visual readback.

## Scope boundary

No new project-specific site, Search, Wordstat, Alice/AI or business-fact acquisition was performed. Quarantined post-hoc evidence was not used.

## Final structure

- ready actions: 3;
- clarification items: 5;
- semantic assignments: 46;
- additional checks: 4;
- unique internal-link pairs: 14;
- bibliography: 10;
- final committed PDF: 14 pages.

## Deterministic and cross-format QA

`RESEARCH_REBUILD_POST_RELEASE_DOCUMENT_02_CLIENT_INSTRUCTION_CLEANUP_QA_2026-09-08.json`

Results:

- client pseudo-action phrase hits: 0;
- obvious open/find/navigate mechanics: 0;
- project-internal traceability leaks: 0;
- standalone Russian `не` hits in current client Markdown: 0;
- client process-meta hits: 0;
- semantic rows: 46;
- internal-link rows: 14;
- MD/DOCX/PDF banned-language hits: 0 / 0 / 0;
- standalone empty `Содержание` heading: 0;
- final source SHA-256: `caf0e01dc6f232299c6edcba46712e7405a81addbbbf324a8670950e53017814`;
- final DOCX SHA-256: `a0eb4672a170bb30320cfadb4d3f225b4d6e7bc39d0c91a7335bcb316535b1d1`;
- final PDF SHA-256: `36de1d6e740ffd281a3707c8f342379c1cc06bec37f31a0eff4833eb923a13b5`.

## Independent final PDF visual readback

The exact workflow artifact produced from the committed PDF was downloaded and all 14 rendered pages were inspected.

Result: **PASS — 14/14**.

Verified:

- no empty TOC heading;
- no orphan mini field labels;
- no clipped or overlapping text;
- no broken glyphs;
- semantic and internal-link tables remain legible across page breaks;
- direct URLs are legible;
- bold implementation examples are visible;
- final bibliography is legible through item 10.

Final materialization commit:

`8c597880f7f9b3b9d051d626b7e6c4efb1e2779b`

NEXT_ACTION = OWNER_REVIEW_CLIENT_CLEANED_DOCUMENT_02__DO_NOT_START_DOCUMENT_03
