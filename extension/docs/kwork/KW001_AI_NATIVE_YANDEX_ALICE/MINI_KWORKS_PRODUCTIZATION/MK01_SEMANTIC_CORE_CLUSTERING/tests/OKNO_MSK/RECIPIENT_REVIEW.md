# MK01 / OKNO_MSK — recipient review

Статус: **PASS AFTER CLIENT-USABILITY CORRECTIONS**

Дата: **2026-09-09**  
Проверяемый файл: `MK01_OKNO_MSK_SEMANTIC_CORE_2026-09-09.xlsx`

## Walkthrough глазами клиента

| Вопрос | Результат | Доказательство в deliverable |
|---|---|---|
| Понятно ли, что перед клиентом? | **PASS** | Первый экран: «Семантическое ядро сайта okno-msk.ru», Москва, Яндекс Вордстат, сбор/чистка/группировка. |
| Понятно ли, какой лист открыть первым? | **PASS** | Первая строка инструкции прямо направляет на «Рабочее ядро»; этот лист стоит третьим сразу после полного audit view. |
| Понятно ли, что является рабочим ядром? | **PASS** | На первом листе показано 2185; лист «Рабочее ядро» содержит только эти 2185 строк, каждая с группой, задачей, intent и основанием. |
| Понятно ли, что исключено? | **PASS** | На первом листе показано 468; отдельный лист «Исключено» содержит все 468 строк. |
| Понятно ли, почему запрос исключён? | **PASS** | Для каждой строки есть русская причина, этап исключения и условие пересмотра. |
| Понятно ли назначение кластеров? | **PASS** | Лист «Группы запросов» даёт задачу пользователя, intent, роль темы, representative, примеры и границу. Пояснено, что группа не равна готовому URL. |
| Сохранена ли неопределённость? | **PASS** | «На проверку» содержит 13 Search-required и 174 deferred строк с причиной и следующим действием. |
| Понятно ли значение чисел Вордстата? | **PASS** | «Методика» объясняет популярные/похожие запросы, Москву, устройства, отсутствие операторов и неаддитивность. |
| Есть ли внутренние термины вместо объяснений? | **PASS** | Основные статусы, причины и поля русские; технические коды вынесены в последние колонки «для аудита». |
| Выглядит ли файл как сырой dump? | **PASS** | 7 специализированных листов, заголовки, заметки, таблицы, фильтры, закрепления, цветовые статусы и краткая инструкция. |
| Можно ли использовать без репозитория? | **PASS** | Scope, counts, основной рабочий набор, группы, ограничения и next steps находятся внутри XLSX и в `DELIVERY_SUMMARY.md`. |
| Не продано ли downstream-решение? | **PASS** | Нет назначения URL, создания/слияния страниц, архитектуры, перелинковки, content TZ, competitor additions или AI conclusions. |

## Что было исправлено по recipient review

1. В первом рендере блок summary был слишком узким: один и тот же worksheet column получил конфликтующие widths в двух секциях. Ширины заданы один раз на уровне листа; итоговый первый экран читается без посимвольного переноса.
2. Алфавитная сортировка роли показывала пять исключённых групп раньше рабочих. В финале сначала идут 54 рабочие группы, затем 5 outside-task groups.
3. В словаре оставался англоязычный термин `provenance`. Он заменён на «история происхождения данных»; технические trace IDs оставлены только в аудиторских колонках.
4. Уточнено Search-пояснение: сохранено 75 решений, из них 66 относятся к exact universe phrases, 9 — к контрольным формулировкам и не добавляют строки.

## Визуальный readback

После исправлений отрендерены и просмотрены все листы:

```text
Как пользоваться   1174×945
Все запросы         3289×953
Рабочее ядро        3136×953
Группы запросов     2875×993
На проверку         3487×953
Исключено           3046×953
Методика            1948×873
```

Проверены заголовки, первые строки, длинные объяснения, numeric display, row banding, отсутствие обрезанного первого экрана и рабочий-first порядок групп.

## Итог

```text
RECIPIENT_CAN_IDENTIFY_SCOPE = true
RECIPIENT_CAN_FIND_PRIMARY_SHEET = true
RECIPIENT_CAN_SEPARATE_WORKING_REVIEW_EXCLUDED = true
RECIPIENT_CAN_UNDERSTAND_CLUSTERS = true
RECIPIENT_CAN_INTERPRET_WORDSTAT_METRICS = true
RECIPIENT_CAN_SEE_PRODUCT_BOUNDARY = true
REPOSITORY_KNOWLEDGE_REQUIRED = false
RAW_DUMP_APPEARANCE = false
RECIPIENT_REVIEW = PASS
```
