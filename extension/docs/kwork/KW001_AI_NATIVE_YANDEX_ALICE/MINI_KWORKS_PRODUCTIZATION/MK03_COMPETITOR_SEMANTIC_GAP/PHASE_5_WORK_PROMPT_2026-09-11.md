# MK03 — PHASE 5 WORK PROMPT — 2026-09-11

## 0. EXECUTION IDENTITY

You are continuing the existing Kwork / Yandex Marketing Bridge productization work.

THIS IS NOT A NEW PROJECT.
THIS IS NOT A NEW PRODUCT DESIGN TASK.
THIS IS NOT MK01, MK02, MK04, MK05, MK06 OR MK07.
THIS IS NOT PHASE 8 PRICING.
THIS IS NOT A REQUEST TO REDESIGN THE MK03 METHOD.

This is **PHASE 5 — MK03-ONLY OKNO_MSK REHEARSAL** for:

**MK03 — SEO-анализ конкурентов в Яндексе + поиск пропущенного спроса и структурных возможностей.**

Repository:

`MaksimUnimax/Yandex_direct`

Branch:

`roadmap/kwork-productization-2026-08-28`

MK03 Level-1 root:

`extension/docs/kwork/KW001_AI_NATIVE_YANDEX_ALICE/MINI_KWORKS_PRODUCTIZATION/MK03_COMPETITOR_SEMANTIC_GAP`

Primary Level-2 source job:

`extension/docs/kwork/KW001_AI_NATIVE_YANDEX_ALICE/tests/OKNO_MSK`

Create/use the isolated rehearsal workspace under:

`extension/docs/kwork/KW001_AI_NATIVE_YANDEX_ALICE/MINI_KWORKS_PRODUCTIZATION/MK03_COMPETITOR_SEMANTIC_GAP/tests/OKNO_MSK`

Phase-4 method publication anchor:

`151802ad17b9f3f651701afadf27fa53829d2f55`

Do NOT assume that anchor is still branch HEAD. Read the CURRENT live remote branch before any write and preserve all concurrent work.

## 1. FIRST ACTION — LIVE RECOVERY / READ IN FULL

Before touching Level-2 data, fetch the CURRENT live branch and read the current versions of the following IN FULL, in this order:

1. `extension/docs/kwork/KWORK_LARGE_ARTIFACT_OWNER_RELAY_AND_PUBLICATION_RULE.md`
2. `extension/docs/kwork/KW001_AI_NATIVE_YANDEX_ALICE/MINI_KWORKS_PRODUCTIZATION/MINI_KWORK_DEVELOPMENT_PROTOCOL.md`
3. `extension/docs/kwork/KW001_AI_NATIVE_YANDEX_ALICE/MINI_KWORKS_PRODUCTIZATION/SERIES_ROADMAP.md`
4. `extension/docs/kwork/KW001_AI_NATIVE_YANDEX_ALICE/MINI_KWORKS_PRODUCTIZATION/YANDEX_ONLY_SCOPE.md`
5. `<MK03>/PRODUCT_ROADMAP.md`
6. `<MK03>/PRODUCT_SCOPE.md`
7. `<MK03>/CLIENT_INPUT_CONTRACT.md`
8. `<MK03>/GENERAL_RULES.md`
9. `<MK03>/ERRORS_AND_LESSONS.md`
10. `<MK03>/SERP_COVERAGE_MODE_DECISION_2026-09-11.md`
11. `<MK03>/STEP_RULES_INDEX.md`
12. every `<MK03>/steps/STEP_00_*.md` through `<MK03>/steps/STEP_09_*.md`
13. `<MK03>/EXECUTION_ROADMAP.md`
14. `<MK03>/DELIVERABLE_SPEC.md`
15. `<MK03>/QA_AND_RELEASE.md`
16. `<MK03>/PHASE_4_METHOD_CONSISTENCY_AUDIT_2026-09-11.md`

Then read the relevant current OKNO_MSK source/evidence authorities needed to execute these rules. Prefer current/corrected/canonical authorities over historical convenience.

Do not start transformations until this reading gate is complete.

## 2. ROLE BOUNDARY

Canonical split:

```text
MAIN CHAT / OWNER-CONTROLLED PRODUCTIZATION
= product identity
+ scope
+ method
+ rules
+ roadmap
+ boundaries
+ pre-data consistency audit

WORK
= execute the already-frozen MK03 method on large Level-2 data
+ materialize Level-2 authorities
+ reconcile evidence
+ run QA
+ build candidate client package
+ measure real workload
+ report any genuine method defect
```

You MUST NOT silently weaken, skip, replace or redesign Level-1 method rules for convenience.

If a genuine rehearsal-discovered Level-1 defect appears, do not patch only one OKNO_MSK example. Record the failure class, root cause, affected Level-1 authorities and full Level-2 impact set; correct the method at class level before declaring PASS.

## 3. REHEARSAL PREMISE

Execute OKNO_MSK **as if the client bought only MK03**.

Do not hand back the full historical KW-001 result under an MK03 label.

Historical/corrected OKNO_MSK evidence may be reused only when compatible with:
- target region;
- evidence type;
- freshness required for the claim;
- product scope;
- provenance/traceability rules.

Excluded downstream products/stages must not contaminate the MK03 answer.

## 4. PROVIDER-CALL POLICY FOR THIS REHEARSAL

Default policy:

```text
REUSE_PRESERVED_EVIDENCE_FIRST = true
NEW_PROVIDER_CALLS_BY_DEFAULT = FORBIDDEN
```

Before considering ANY new Yandex Search or Wordstat call:

1. search the preserved OKNO_MSK evidence fully;
2. name the exact unresolved information gap;
3. name the exact MK03 decision that cannot be made without it;
4. explain why preserved evidence is insufficient/stale for that claim;
5. confirm that a new call is explicitly allowed by owner/main-chat authority for this rehearsal.

If authorization is absent, preserve `HOLD_EVIDENCE / SEARCH_REQUIRED / UNKNOWN` rather than inventing certainty.

Do not use Google, third-party reverse-domain databases or AI/Alice evidence to fill MK03 gaps.

## 5. MANDATORY MK03 EXECUTION CHAIN

Execute the current Level-1 roadmap exactly:

```text
STEP 00  SCOPE + CURRENT-SITE BASELINE
STEP 01  REPRESENTATIVE YANDEX DISCOVERY BASELINE
STEP 02  REAL ORGANIC COMPETITOR DISCOVERY
STEP 03  EVIDENCE-BEARING COMPETITOR-PAGE INSPECTION
STEP 04  COMPETITOR-DERIVED TOPIC / SEED HYPOTHESES
STEP 05  WORDSTAT EXPANSION + NORMALIZATION / SANITATION
STEP 06  MATERIAL CURRENT-SEARCH CONFIRMATION
STEP 07  CLIENT COVERAGE COMPARISON + GAP CLASSIFICATION
STEP 08  BOUNDED STRUCTURAL OPPORTUNITY INTERPRETATION
STEP 09  CLIENT MATERIALIZATION + QA
```

Permanent non-equivalences that MUST survive every table/report:

```text
BUSINESS RIVAL != ORGANIC SEARCH COMPETITOR
COMPETITOR PAGE TOPIC != EXACT QUERY RANKING
COMPETITOR-DERIVED SEED != ACCEPTED KEYWORD
RAW WORDSTAT != ANALYTICAL CANDIDATE
NEW PROVENANCE != NEW KEYWORD ID
UNPROBED QUERY != SEARCH-CONFIRMED QUERY
TESTED QUERY VISIBILITY != FULL DOMAIN KEYWORD UNIVERSE
CONFIRMED GAP != AUTOMATIC CREATE PAGE
ALREADY_COVERED != USELESS RESULT
MK03 DISCOVERY BASELINE != FULL MK01
```

## 6. CURRENT-SITE FRESHNESS

A semantic gap claim depends on current-enough client coverage.

Do not infer current absence solely from an old inventory/report.

```text
OLD ABSENCE != CURRENT SITE ABSENCE
SEARCH ABSENCE != SITE ABSENCE
```

Where preserved current-site evidence is sufficient, reuse it. Where the claim cannot be made safely without fresh public-site recheck and such inspection is available without a prohibited provider call, recheck the public site. Preserve uncertainty when current evidence cannot be established.

## 7. REQUIRED LEVEL-2 AUTHORITIES

Materialize machine-readable/canonical authorities sufficient to support the whole sold chain. Use repo conventions for exact filenames, but the rehearsal must contain equivalent authorities for at least:

1. `TEST_ORDER.md` — scope, input, region, product-only premise, exclusions.
2. current-site baseline / coverage authority.
3. representative discovery query-family authority.
4. real Yandex organic competitor discovery register.
5. evidence-bearing competitor-page register.
6. competitor-derived seed/provenance register.
7. Wordstat raw/reused evidence manifest as applicable.
8. normalized/sanitized candidate authority.
9. exact tested query→competitor Search matrix.
10. client coverage + final gap decision register.
11. bounded opportunity register.
12. acquisition/final-state accounting reconciliation.
13. provider-call/reuse accounting receipt.
14. `TEST_EXECUTION.md` — what actually ran and in which order.
15. `TEST_RESULT.md` — final analytical result and counts/states.
16. `CLIENT_DELIVERABLE.md` — client-package content map / handoff authority.
17. `QA.md` — gate-by-gate result.

Names may be more specific, split, or combined where justified by repository convention, but do not omit the logical authority.

For each canonical row/entity preserve enough lineage to reconstruct the applicable chain:

```text
SEARCH FAMILY / QUERY
→ COMPETITOR
→ EVIDENCE PAGE
→ TOPIC / SEED
→ WORDSTAT / DEMAND EVIDENCE AS APPLICABLE
→ SEARCH-TESTED STATE AS APPLICABLE
→ CLIENT COVERAGE EVIDENCE
→ FINAL GAP STATE
→ OPPORTUNITY / HOLD REASON
```

## 8. GAP STATES / NO-SILENT-DROP ACCOUNTING

Every material candidate ends in an explicit state equivalent to:

```text
CONFIRMED_GAP
ALREADY_COVERED
REJECT_OFF_SCOPE
HOLD_EVIDENCE
```

Deterministic duplicate collapse is allowed only with preserved lineage.

No unexplained disappearance is allowed.

Produce an accounting reconciliation proving where every material acquired candidate ended.

## 9. SERP COVERAGE MODE

Freeze:

```text
SERP_COVERAGE_MODE = SELECTIVE_DECISION_SERP
```

Only exact query→competitor relations actually checked in current/preserved-current Yandex Search may be labelled Search-tested.

Do NOT generalize tested examples into:
- full domain ranking coverage;
- full keyword universe;
- ranking proof for all competitor-derived page topics.

The client package must make tested vs unprobed/inferred evidence visibly distinguishable.

## 10. BOUNDED OPPORTUNITY INTERPRETATION

For each `CONFIRMED_GAP`, determine only the bounded consequence supported by evidence, for example:
- topic/content opportunity;
- strengthening an existing page/direction;
- possible page/section opportunity;
- route to later MK02/MK04/MK05 work;
- no structural action yet because evidence is insufficient.

Do not build a full target site architecture or implementation-ready TZ in MK03.

`ALREADY_COVERED` is a positive validated result and must remain visible where client-useful.

## 11. CLIENT PACKAGE TO MATERIALIZE

Base physical package is fixed:

```text
1 XLSX
+ 1 ANALYTICAL PDF
+ SHORT HANDOFF MESSAGE
```

Do NOT create a separate implementation-TZ PDF.

### XLSX

The workbook must expose equivalent logical views for:
- start/guide;
- actual Yandex search competitors;
- evidence-bearing competitor pages;
- competitor-derived candidates + provenance;
- Wordstat demand validation;
- actually tested query→competitor Search matrix;
- client coverage + final gap states;
- bounded opportunities / next actions;
- unresolved HOLD items where material.

Exact sheet count/names may follow generator design. Do not use fragile fixed sheet-count or fixed row-count assumptions.

### Analytical PDF

The PDF must explain the target client result in plain language:
- scope/region;
- principal actual organic competitors;
- material competitor evidence/pages;
- genuinely new in-scope directions;
- Wordstat-supported demand;
- representative Search-tested examples clearly separated from page-topic inference;
- confirmed gaps vs already-covered/off-scope/HOLD;
- highest-value bounded opportunities;
- limitations;
- pointer to row-level XLSX evidence.

Do not dump every row into the PDF.

### Short handoff

Explain:
- what files are included;
- where detailed evidence is;
- where conclusions are;
- key result categories;
- unresolved uncertainty;
- that a gap/opportunity is not automatically an implementation decision.

## 12. GENERATORS / REPEATABILITY

Do not create one-off hand-edited final files when a generator/source authority is appropriate.

If new workbook/PDF generators or validators are needed, put them in the repository using existing conventions and make the final artifacts reproducible from canonical authorities.

Fix source/generator first when a client artifact defect is found; regenerate and re-QA.

## 13. XLSX QA — EXACT FINAL FILE

Follow the platform spreadsheet artifact rules/tools available in Work.

For the exact final workbook:
- inspect every intended sheet and visible client cells;
- inspect headers, filters, freeze panes, widths/readability;
- validate formulas/references if any;
- inspect raw workbook XML/table metadata where needed;
- check hidden critical columns/data, broken tables, clipped key fields, accidental internal jargon/IDs;
- verify cross-view states/totals against canonical authorities;
- after any final workbook change, rerun affected QA.

No fixed universal sheet count, competitor count, gap count or row count.

## 14. PDF QA — EXACT FINAL BYTES

Follow the platform PDF artifact rules/tools available in Work.

For the exact final PDF:
- parse/extract text sufficiently to catch missing/truncated content;
- render EVERY exact final page;
- visually inspect EVERY rendered page;
- reject clipping, overlap, blank/missing pages, broken fonts, unreadable tables and orphaned headings;
- if final PDF bytes change for any reason, rerender/reinspect the final file.

No fixed universal page count.

## 15. RECIPIENT TASK TESTS

Before PASS, independently test whether a recipient without repository access can answer:

1. Which sites were actually observed as Yandex organic competitors and for what search families?
2. Which competitor page exposed a selected candidate direction?
3. What Wordstat evidence supports that direction, if applicable?
4. Was a selected exact query→competitor relation actually Search-tested or only page-topic-derived?
5. Is the selected direction `CONFIRMED_GAP`, `ALREADY_COVERED`, `REJECT_OFF_SCOPE` or `HOLD_EVIDENCE`, and why?
6. What bounded client opportunity follows?
7. What remains uncertain and exactly what evidence would resolve it?

Counts-only output fails.

## 16. FAILURE / REGRESSION TESTS

The Phase-5 QA must explicitly reject at least these recurring failure classes:

```text
CLIENT COMPETITOR HINT AS ORGANIC COMPETITOR TRUTH
PAGE TOPIC AS EXACT QUERY RANKING EVIDENCE
RAW WORDSTAT DIRECTLY APPENDED
NEW PROVENANCE DUPLICATED AS NEW KEYWORD
UNPROBED QUERY CALLED SEARCH-CONFIRMED
SELECTIVE SERP GENERALIZED INTO FULL DOMAIN UNIVERSE
OLD CLIENT-SITE ABSENCE USED AS CURRENT FACT
CONFIRMED GAP AUTO-CONVERTED TO CREATE PAGE
ALREADY-COVERED FINDINGS DROPPED AS "NO ACTION"
SILENT DROPS / UNRECONCILED COUNTS
SUMMARY COUNTS WITHOUT INSPECTABLE RESULT
FINAL XLSX/PDF NOT PHYSICALLY INSPECTED
```

## 17. SEMANTIC-BLOCK CHECKPOINT PERSISTENCE — MANDATORY

Do NOT keep the whole rehearsal only in Work memory/scratch until the end.

Do NOT commit every row or every minute.

Persist when a meaningful internally consistent block is complete and recoverable.

Required checkpoint logic for this rehearsal should follow equivalent semantic boundaries:

```text
A. INPUT / SOURCE / PRESERVED-EVIDENCE AUDIT COMPLETE
→ B. COMPETITOR DISCOVERY + PAGE EVIDENCE AUTHORITY COMPLETE
→ C. SEED / WORDSTAT / NORMALIZED CANDIDATE AUTHORITY COMPLETE
→ D. SEARCH-TESTED + CLIENT-COVERAGE + GAP DECISION AUTHORITY COMPLETE
→ E. OPPORTUNITY + ACCOUNTING / FINAL ANALYTICAL AUTHORITY COMPLETE
→ F. CLIENT XLSX/PDF SOURCES + GENERATORS COMPLETE
→ G. EXACT FINAL ARTIFACTS + QA COMPLETE
→ H. FINAL STATE / REMOTE READBACK COMPLETE
```

For each completed material block:

```text
SAVE MATERIAL OUTPUTS
→ RECORD CURRENT CURSOR / COMPLETE / REMAINS
→ LOCAL QA
→ PUBLISH VIA APPROVED TRANSPORT
→ REMOTE READBACK
→ CONTINUE FROM REMOTE-RECOVERABLE STATE
```

If one logical block is too large, split it only at a natural recoverable boundary.

## 18. ARTIFACT PUBLICATION / TRANSPORT POLICY — OWNER-LOCKED

Freeze exactly:

```text
ARTIFACT_PUBLICATION_POLICY = NATIVE_GIT_IF_ALREADY_AUTHENTICATED | OWNER_RELAY_IF_MORE_EFFICIENT
LARGE_ARTIFACT_MODEL_TRANSPORT = FORBIDDEN_BY_DEFAULT
OWNER_RELAY_ALLOWED = true
REMOTE_READBACK_REQUIRED = true
```

Preferred order:

```text
A. Native authenticated Git push — only if already available/reliable/inexpensive
ELSE
B. Owner-relay web publication — approved normal route
NEVER BY DEFAULT
C. Giant model/connector/base64/chunk transport of finished large files
```

If owner relay is needed:
- finish/generate/QA files locally first;
- freeze exact filenames and hashes;
- provide downloadable files, plus a convenience ZIP when useful;
- clearly state whether ZIP is transport-only and must not be committed;
- give target repo/branch/directory and direct GitHub upload page when possible;
- owner uploads through normal authenticated browser UI;
- after owner confirmation, read remote state and verify identity/QA;
- do not claim publication complete before remote readback.

Never request GitHub password, PAT, 2FA code or private SSH key in chat.

## 19. SHARED-BRANCH CONCURRENCY SAFETY

The branch is shared with active KW002/other work.

Mandatory:
- read live remote HEAD before each publication block;
- preserve unrelated concurrent changes;
- never force push;
- never reset branch to an older remembered SHA;
- when using low-level Git data operations, build commits on the current remote parent;
- after publish, verify actual resulting remote HEAD/state;
- if branch moved, rebase/reapply only the intended MK03 payload on the new current tree.

## 20. PHASE-5 OUTPUT / COMPLETION REPORT

At the end, report concretely:
- what Level-2 authorities were created/updated;
- exact rehearsal scope/region;
- competitor/page/candidate/gap state totals derived from canonical authorities;
- number of actually Search-tested exact pairs;
- preserved evidence reused;
- new Search calls and Wordstat calls (expected default: 0 unless explicitly authorized);
- unresolved HOLD/SEARCH_REQUIRED items;
- client XLSX path + SHA256;
- analytical PDF path + SHA256;
- generator/source paths;
- QA gate results;
- commits/checkpoints;
- final remote HEAD;
- remote readback result;
- any genuine Level-1 defect found and corrected.

Do not merely provide a plan or review. Execute the full Phase-5 rehearsal through candidate client package + QA + publication/readback within the allowed evidence/transport boundaries.

## 21. STOP BOUNDARY

DO NOT START:
- Phase 8 pricing/economics;
- Kwork card;
- portfolio/cover visuals;
- owner marketplace publication;
- another mini-kwork.

When Phase 5 has passed, stop with a precise handoff to the next MK03 productization phase. Do not silently continue beyond the accepted rehearsal scope.
