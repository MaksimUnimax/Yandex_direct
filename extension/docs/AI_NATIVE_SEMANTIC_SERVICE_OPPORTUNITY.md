# AI-NATIVE SEMANTIC REBUILD — OWN SERVICE OPPORTUNITY

Status: **STRATEGIC OFFER / PRODUCT-DISCOVERY EVIDENCE / NO `extension/src` CHANGE AUTHORIZED**  
Date: 2026-08-27  
Internal offer id: **O-001**

## 1. What this is

This is our own service concept derived from the `MaksimUnimax/blood_sand` marketing strategy and real research workflow. It is **not** a freelance-marketplace observation and therefore must not be counted as F-018 in market-demand statistics.

Working internal shorthand may remain `ИИ-индексация`, but the client promise should not imply guaranteed indexing, ranking or inclusion in AI answers.

Safer client-facing formulations:

- `Пересборка семантического ядра под Яндекс + Алису AI`;
- `AI-native семантическое ядро: поиск + Алиса AI`;
- `Семантика для обычного и генеративного поиска`.

The core distinction is not generic GEO/AEO formatting. It is an **evidence-first rebuild of the site's semantic and page architecture using both human-search demand and observed AI-search behavior**.

## 2. Operating model — ChatGPT Plus is the worker, the extension is the hands

All capability judgments for O-001 use this system boundary:

```text
CLIENT
→ gives site / business / region / goals / access where required

CHATGPT PLUS
→ plans the research
→ decides which queries are worth measuring
→ interprets Wordstat, Search, Alice, Webmaster and Metrika evidence
→ detects contamination / contradictions / missing evidence
→ clusters by actual user job
→ scores H / A / C / O
→ chooses page jobs
→ rebuilds the semantic architecture
→ writes recommendations / briefs / reports
→ performs QA

YANDEX MARKETING BRIDGE
→ is the controlled set of hands for authenticated/provider measurements
→ Wordstat
→ Yandex Search API
→ Webmaster
→ Metrika
→ Direct where commercially useful

HUMAN OPERATOR
→ authorizes/runs the extension and supplies account access
→ does NOT replace ChatGPT as SEO analyst or manually perform the expert reasoning
```

This distinction is permanent for our freelance capability analysis: a feature does not need to be autonomous inside the extension if ChatGPT Plus can perform it reliably from evidence the extension acquires. The extension should productize acquisition, persistence, safety and repeatability; ChatGPT remains the reasoning layer.

## 3. Blood & Sand source methodology

The accepted `blood_sand` strategy treats ordinary Yandex Search visibility and Alice AI visibility as one connected system rather than `SEO first, GEO later`.

Its evidence model separates source provenance instead of inventing one fake universal frequency. Important source types include:

```text
SEED
WORDSTAT
SERP_QUERY
ALICE_INPUT
ALICE_FANOUT_OBSERVED
ALICE_FANOUT_INFERRED
ALICE_SOURCE
WEBMASTER_SEARCH
WEBMASTER_ALICE
CUSTOMER_EVIDENCE
MARKETPLACE_EVIDENCE
```

The canonical data flow is:

```text
registry/input
→ raw evidence
→ normalized observations
→ Query Evidence Ledger
→ derived analysis / decisions
```

For each cluster the strategy keeps four reasons for priority separate:

```text
H = Human demand
A = Alice importance
C = Commercial value
O = Owned-asset value
```

These are not blindly summed into one opaque score.

The final decision is not simply `keyword → article`. ChatGPT chooses the actual page job:

```text
new landing/page
existing-page expansion
FAQ / answer block
comparison / guide
product fact / attribute
supporting content
no page at all
```

## 4. Why this is materially different from an ordinary semantic core

A normal semantic core mainly answers:

> What do people type into search, how often, and how should those queries be grouped?

O-001 additionally asks:

> For the same topic, what job does ordinary Search solve, what job does Alice AI solve, which sources/pages does AI rely on, what additional fan-out/questions appear, and what content/page architecture should the client build to compete in both systems without sacrificing commercial value?

`blood_sand` already produced direct evidence that Search and Alice can solve different jobs for the same root query. Examples in the accepted cross-surface review include:

- a root where Search is strongly transactional while Alice is meaning/history/use-first;
- roots where Search is commercial-first while Alice emphasizes mythology, explanation or suitability;
- broad high-frequency roots whose apparent opportunity collapses after intent contamination is compared across Wordstat, Search and Alice.

Therefore an ordinary keyword list can make the wrong page decision even when its frequency data is correct.

## 5. Current market differentiation

Generic GEO/AEO services already exist on Kwork. Current examples include AI-readiness audits, FAQ/content recommendations, `atomic answer` optimization, LSI headings, and mass AI-oriented article production.

The quick market review did **not** surface an exact equivalent that explicitly combines all of the following as one evidence chain:

```text
Wordstat measurements
+ direct Yandex SERP measurements
+ direct Alice answer/source/fan-out observations
+ strict observed-vs-inferred provenance
+ Query Evidence Ledger
+ H/A/C/O decision framework
+ page-job / target-page architecture
+ post-launch Webmaster Alice SoV validation
```

This is a differentiation signal, not proof that no competitor anywhere offers a similar methodology.

## 6. Current capability with ChatGPT Plus + accepted Bridge

| Layer | Current status | Who does the work |
|---|---|---|
| Site/business/offer analysis | **YES** | ChatGPT Plus |
| Seed generation and hypothesis map | **YES** | ChatGPT Plus |
| Wordstat human-demand acquisition | **YES** | Bridge `WORDSTAT_API_V1` |
| Dynamics / region / device evidence | **YES where current Wordstat methods expose it** | Bridge + ChatGPT |
| Direct regional Yandex SERP evidence | **YES** | Bridge `SEARCH_API_V1` |
| Competitor/source/page-type analysis | **YES bounded** | ChatGPT Plus from Search/public evidence |
| Alice query-set design | **YES** | ChatGPT Plus |
| Alice answer/source/fan-out interpretation | **YES** | ChatGPT Plus once evidence is captured |
| Reproducible direct Alice capture through current Bridge | **MISSING** | missing hand |
| Query Evidence Ledger construction | **YES** | ChatGPT Plus/artifact layer |
| H/A/C/O assessment | **YES** | ChatGPT Plus |
| Semantic clustering by actual user job | **YES** | ChatGPT Plus |
| Keyword/topic → page-job mapping | **YES bounded** | ChatGPT Plus |
| XLSX/CSV/PDF/DOCX deliverables | **YES** | ChatGPT Plus artifact tooling |
| Ordinary post-launch Webmaster query performance | **YES** | current Bridge Webmaster slice |
| Alice SoV/query/source-page data through current Bridge | **MISSING in current Webmaster protocol** | missing hand/import path |
| Post-launch traffic/conversion analysis | **YES where Metrika data exists** | Bridge + ChatGPT |
| Direct account data | **OPTIONAL, NOT REQUIRED** | Bridge Direct after provider approval |

Important current code boundary: accepted Webmaster exposes `listHosts`, `getSummary`, `getDiagnostics`, `getPopularQueries`; it does not currently expose the new `Видимость сайта в Алисе AI` analytics surface.

## 7. Verdict

### Strategic/service verdict

**YES — STRONG DIFFERENTIATED PREMIUM OFFER.**

It is a natural premium layer over the market-proven Semantic Core Builder and uses a methodology already exercised in `blood_sand` rather than an invented marketing label.

### Strict current toolchain verdict

**PARTIAL for the exact zero-manual-expert operating model today.**

The blocker is narrow and concrete:

```text
ChatGPT can do the analysis.
Wordstat hand exists.
Search hand exists.
Webmaster ordinary-search hand exists.
Metrika hand exists.

Missing hand = repeatable Alice observation acquisition
and post-launch Alice-visibility acquisition/import.
```

A bounded service can still be delivered today if canonical Alice observations and/or Webmaster Alice exports are supplied through a controlled browser/export capture path. But the fully repeatable target state — operator only runs the extension while ChatGPT performs everything else — requires productizing the Alice evidence hand.

## 8. Exact O-001 workflow

```text
1. Intake
   site + region + products/services + commercial goals + existing semantic core + optional competitors/pages.

2. Site/business understanding
   ChatGPT maps the real offer, audience, current page architecture, exclusions and conversion jobs.

3. Human-demand layer
   Bridge Wordstat → GetTop / dynamics / regional-device evidence where decision-useful.
   ChatGPT builds and cleans the ordinary semantic candidate set.

4. Ordinary Search layer
   ChatGPT selects representative roots and decision-useful secondaries.
   Bridge Search measures actual Yandex SERPs in the target region.
   ChatGPT records domains, page types, marketplace-vs-independent composition, informational/commercial job and competitor patterns.

5. AI test-set design
   ChatGPT does NOT send every keyword blindly to Alice.
   It selects queries that resolve real uncertainty: commercial vs informational split, selection/use-case branches, comparison branches, contaminated broad roots, missing questions.

6. Alice observation
   one clean/new conversation/context per root;
   capture answer presence/snapshot;
   capture actual source domains/URLs/page types;
   capture observed fan-out when exposed;
   store inferred fan-out separately;
   invalidate context-contaminated observations instead of forcing them into the conclusion.

7. Evidence merge
   ChatGPT creates/updates the Query Evidence Ledger and keeps provenance for every material conclusion.

8. Decision analysis
   per cluster: H / A / C / O;
   compare Search job vs Alice job;
   identify source/content gaps and commercial fit.

9. Semantic rebuild
   ChatGPT decides for every important cluster:
   existing page / new page / FAQ / comparison / guide / product fact / reject.
   Resolve cannibalization and page-role conflicts.

10. Client deliverables
    AI-native semantic workbook;
    Search-vs-Alice intent-gap matrix;
    source/competitor map;
    page/content architecture;
    priority action plan;
    evidence/methodology notes;
    optional page briefs/content specifications.

11. Post-launch validation
    ordinary Webmaster search performance;
    Alice visibility / SoV / source pages when available through capture/import;
    Metrika behavior/conversion;
    compare before/after and revise page jobs when evidence contradicts the initial hypothesis.
```

## 9. Recommended client artifact

Core workbook columns should be closer to this than to a traditional two-column keyword list:

```text
query
cluster
intent
source_types
wordstat evidence
SERP job
SERP leading page/source types
Alice answer status
Alice job
Alice source domains/pages
fanout_observed
fanout_inferred
H
A
C
O
current page
recommended page job
action
priority
confidence / evidence status
notes
```

This preserves the most valuable part of the method: why a page/action was recommended.

## 10. Missing product capability — highest leverage hand

### AI SEARCH / ALICE OBSERVATION JOB

Target:

```text
query registry
→ clean isolated Alice observation
→ answer snapshot
→ source URLs/domains/page types
→ observed fan-out
→ timestamp/context metadata
→ raw immutable evidence
→ normalized observation
→ safe resume / review
```

Requirements:

- never treat source display order as ranking unless the source explicitly supports that interpretation;
- never convert inferred fan-out into observed evidence;
- preserve context so contaminated runs can be rejected;
- repeated measurements create new observations rather than overwriting history;
- do not promise deterministic source presence because AI answers and source sets can change.

### WEBMASTER ALICE VISIBILITY CAPTURE / IMPORT

Target post-launch evidence:

```text
Share of Voice
query examples
source/page presence
competitor/source environment
dynamics
```

The current accepted Webmaster protocol does not expose this surface, so the first implementation can be a controlled export/import or browser-assisted capture if there is no suitable stable official API path.

No provider/interface implementation is authorized while Phase 5 Direct owner-live is pending.

## 11. Product position relative to Semantic Core Builder

Do not replace the P0 Semantic Core Builder with O-001. Use a tiered service architecture:

```text
BASE
Semantic Core Builder
= human search demand + cleaning + grouping + page mapping

PREMIUM
AI-Native Semantic Rebuild
= BASE
+ Yandex SERP evidence
+ Alice evidence
+ Search-vs-AI intent gaps
+ source competition
+ H/A/C/O
+ AI-aware page jobs
+ post-launch AI visibility measurement
```

This separation is commercially useful because AI measurement/research adds meaningful operator/model work and should not be silently bundled into low-price 100-key semantic packages.

## 12. Commercial boundaries

Never promise:

- guaranteed indexing in Alice AI;
- guaranteed inclusion as a source;
- guaranteed SoV growth;
- guaranteed traffic/sales from an AI mention;
- that low Wordstat frequency means low AI importance;
- that high AI importance automatically means a new page must be created;
- that observed Alice source order is a ranking;
- that one AI answer is permanently reproducible.

Promise what we can control:

- evidence collection under a declared methodology;
- a transparent semantic/page rebuild based on both search and AI evidence;
- concrete content/page actions;
- before/after measurement where client data exists;
- revision of recommendations when new evidence contradicts the original hypothesis.

## 13. Product conclusion

O-001 should be treated as a **strategic premium differentiator** of Yandex Marketing Bridge.

The strongest architecture is not `extension does SEO automatically` and not `human SEO specialist manually works around the extension`.

It is:

```text
ChatGPT Plus = analyst / planner / semantic architect / QA
Yandex Marketing Bridge = authenticated measurement and execution hands
Human = owner/operator/authorization boundary
```

Under this model the current product already supplies most of the data primitives. The main missing hand for the premium AI-native service is Alice observation + Alice-visibility acquisition. Once that hand is reliable, the Blood & Sand methodology becomes a repeatable freelance service rather than a one-project research process.
