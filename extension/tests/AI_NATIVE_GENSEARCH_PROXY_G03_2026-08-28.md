# AI-native GenSearch proxy validation — G03

Date: 2026-08-28

Root: `вегвизир`

## Provider execution

```text
protocol = SEARCH_API_V1
method = genSearch
request_id = search-cb28888b-2ddb-4a6d-a654-afea69ed4632
status = OK
http_status = 200
elapsed_ms = 2866
request_executed = true
automatic_retry = false
transport.wire_format = json_array
transport.frame_count = 1
estimated_rub = 5.08
```

Exactly one provider request was observed for this root and no automatic retry occurred.

## GenSearch observation

The answer is strongly history/meaning/correction-first. It explains:

- Vegvísir as an Icelandic magical stave / wayfinder;
- the etymology from `vegur` + `vísir`;
- the eight-rayed visual form;
- the earliest documented description in the Huld manuscript from 1860;
- the key correction that a connection to the Viking Age is not confirmed;
- modern use in tattoos and jewelry;
- an explicit scientific caveat.

Structured sources returned 5 entries. Two were marked `used=true`: Wikipedia and a specialist explanatory article. Other sources included community, marketplace and specialist content pages but were `used=false`.

Observed GenSearch query evidence is narrow:

```text
searchQueries = ["вегвизир"]
```

Hints expand to modern use, related symbols and legends. Hints remain separate GenSearch provenance and are not consumer-Alice fan-out.

## Canonical consumer-Alice comparator

Frozen Pass B records the broad `вегвизир` root as history/meaning-first and explicitly highlights the same decision-critical factual correction: the Huld manuscript (1860), non-Viking-age framing, etymology, purpose and scientific caveat. It therefore promotes the history/meaning guide to launch-critical priority.

Canonical reference:

`extension/tests/AI_NATIVE_BLOOD_SAND_AI_NATIVE_PASS_B_2026-08-27.md`

## Comparison

```text
answer_job_orientation = SAME_OR_STRONGLY_ALIGNED
factual_correction_orientation = SAME_OR_STRONGLY_ALIGNED
historical_provenance_requirement = SAME_OR_STRONGLY_ALIGNED
commercial_vs_explanatory_orientation = SAME_OR_STRONGLY_ALIGNED
source_type_orientation = SUFFICIENTLY_ALIGNED
exact_source_url_overlap = NOT_ASSERTED
consumer_fanout_vs_genSearch_searchQueries = GEN_SEARCH_NARROWER
material_page_job_implication = SAME
```

The material decision is preserved exactly where it matters: the owned Vegvisir asset must be history/meaning/provenance capable, and the common Viking-age framing must be explicitly corrected rather than repeated as folklore fact.

## G03 verdict

```text
classification = SAME_OR_STRONGLY_ALIGNED
comparable = true
systematic_contradiction = false
proxy_signal_useful = true
source_auditability = sufficient_for_this_root
```

G03 is strong evidence that GenSearch can preserve a factual-provenance-sensitive AI-search job rather than merely mirror commercial Search results.