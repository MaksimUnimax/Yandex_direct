# B12 — сверка команд, ограничений и покрытия приёмки

Дата: 2026-09-12. Ветка: `wip/ymb-file-delivery-patch-a-2026-09-12`.
**Доступная проверка B12 выполнена и сохранена. Независимый финальный gate НЕ выполнен. RELEASE_ALLOWED = NO.**

## 1. Точный вход и продолжение

Начальный live HEAD: `f52da796ca399c53c012902f04f49632d2ab6600`. Прочитаны cursor B11 и обязательные patch/resource/pre-delivery правила. Exact owner014 ZIP: `b812c4c54d1054d63a24ea852e1721e813d133172487c9872bac347998e3e53f`, 53 файла.

Через существовавший read-only workflow получен снимок B11; 138 файлов совпали с Git blob SHA. Применены сохранённые materializers, поскольку готового B11 в текущей среде не было. Это точное восстановление байтов, не новая разработка A/B1–B11.

Входное дерево B11: `5fd8a55e980fa54fe5371a51070e9e899307319c8f1ee3255fdc0857b31ebd26`.
Итог B12: `73e6ed85bfd843bf2590a2d1a44a9d04224ce5f63563d127bcd35c2ad092eeec`, 67 файлов.

## 2. Что сверено и что изменено

Проверены реальные команды, псевдонимы, длина запросов, группировка, число результатов, локальный reparse/export и фактический manifest. Существующий валидатор уже проверяет 400 Unicode-кодовых точек и 40 частей по пробельным символам: новый дублирующий валидатор не добавлен. Подсчёт слов на сервере не выдаётся за известный алгоритм.

Официальный контракт допускает 100 групп и 3 документа в группе, но отдельно ограничивает возвращаемую выдачу 250 результатами. Параметры не урезаны молча. Проверены 250 и 251 строки в настоящем вызываемом нормализаторе: 251 не становится усечённым успехом, исходный ответ остаётся сохранённым.

`collectReady` — псевдоним ограниченного collectN, не сбор всех готовых данных. Все пять сетевых псевдонимов в фактическом кандидате отклоняются до provider-вызова: operation-host в manifest отсутствует. Команда из чата не может включить permission. Проверены и локальные действия при выключенном сетевом маршруте.

Найдено несоответствие документации: старые README описывали Phase 1/0.1.0, только Wordstat. Исправлены **ровно README.md и README.txt** внутри будущего кандидата. Теперь описаны owner014, прежние сервисы, новые локальные действия, отключённый deferred network и незавершённая безопасность. Версия manifest/package/product НЕ повышалась для видимости готовности.

**Все остальные 65 файлов, включая 61 JS, byte-identical B11.** Исполняемый код, host permissions, credentials, store/policy, recovery, file transport, main roadmap-ветка и данные заказов не менялись. Новые test/helper файлы остаются только в QA.

## 3. Доказательства и зависимости

| Участок | Проверка | Граница |
|---|---|---|
| README -> фактические protocol/worker/manifest | 24 новых контрактных теста, реальный initializer и локальные действия | Не установленный браузер |
| Границы query/group/result | Настоящие наследуемый SearchProtocol и async normalizer; полный normalizeSaved/raw retention | Сетевая семантика провайдера и его tokenizer не измерены |
| Host capability и injection | Пять alias, no startup fetch, поля permission/providerEnabled отвергаются | Реальный host/auth не включён и не проверен |
| Предыдущие полные пути/модули/export | Сохранённые проверки заново выполнены на B12 | Это сфокусированные пересекающиеся development-наборы, не весь исходный extension/tests |
| Identity/materializer | 67/67 fresh, отказ неверным/непустым входам | Source identity не заменяет финальный ZIP |
| Chrome/MV3/IDB/DOM/ресурсы | Новых PASS нет; docs-only delta не меняет выполняемые байты | Требования объединённого кандидата остаются открытыми |

## 4. Фактически исполнено

Среда: Node v22.16.0. Исполняются сохранённые worker/module JS; Chrome API, IndexedDB, сеть и время представлены controlled fixtures. Реальные provider calls: 0.

- До исправления README: **23/24**, одна ошибка документации.
- После исправления: **24/24**, 249.761093 ms. Это RED/GREEN документации, не OOM или новый runtime bug.
- В свежем exact B12: **99/99** full-worker/content/contract, skipped/cancelled/failed 0, 2311.128512 ms.
- Сохранённые модульные регрессии: **254/254**, 969.347403 ms.
- Сохранённые Manual/export/Patch A: **84/84**, 190.123781 ms.
- Syntax: **61/61 JS**; два JSON разбираются.
- Fresh identity: **67/67**; отрицательные materializer-проверки: **2/2** (непустой output, неверный B11 до записи).
- Кампания `b12_available_campaign.py`: exit 0, **PASS_AVAILABLE_SCOPE**, candidate unchanged.

Не суммировать 99/254/84 как новые уникальные поведения. Старые PASS не присвоены новому запуску. Полный независимый Codex gate не запускался.

## 5. Полные журналы и воспроизведение

`evidence/B12_RAW_LOGS.tar.xz` содержит 20 файлов: фактические RED/GREEN, полные 99/254/84 TAP, stdout/stderr, результаты/статические проверки, fresh identity, source trace, карту покрытия и SHA256SUMS. Это архив evidence без installable кода. Проверено 19 внутренних контрольных сумм и точный round-trip архива.

SHA-256: `9180183ea79110532b34a274534a4f5624337116e48ff79dbe1d7b2bf4170c58`.
Git blob: `2cf27d1ce40507f8c49adcbf9cc6d6a7fd4b553e`, 23936 bytes; ответ GitHub create_blob совпал с локальным расчётом.

Шесть опубликованных source/doc/test/materializer/manifest файлов сверены по Git blob: 6/6 на сохранённых refs до `db88cad9b3872c20e0263edf418f42fe029a827d`. Сводка: `evidence/B12_TEST_RESULTS.json`. Исходники и тесты сохранялись отдельными коммитами до этого отчёта; archive/report/results/cursor закрепляются совместно.

```text
python qa/prepare_b12.py <exact-b11-qa> <empty-b12-qa>
python qa/b12_available_campaign.py <owner014> <exact-b12-qa> <empty-logs>
```

Materializer SHA-256: `4d336a81356ee6186d90e330cab9a4f87eb614e493978e07ef02c08b03b761c5`.
Campaign SHA-256: `69e82a0f20231391f483615727837fd41b79140c87c4d89539d11dcc54a37404`.
B12_INPUTS SHA-256: `c1b6478ef9fd6807576f29bd08fbbc3c41577b3f244b0b6f4c885bafec55debe`.

## 6. Покрытие финальной приёмки — без подмены

Создано дополнение `extension/docs/YMB_ASYNC_FILE_PRE_DELIVERY_COVERAGE_2026-09-12.md`: все PD-00…PD-17 сопоставлены с доступными тестами и конкретно отсутствующей проверкой. Исходный gate и правило безопасности не ослаблены.

Открыты не только браузер и упаковка: **полный исходный regression suite extension/tests ещё не инвентаризирован и не выполнен этой patch-кампанией**. В том числе нельзя объявлять покрытыми все Wordstat-методы, весь Debug ON/OFF и backup/import только по частичным patch-тестам. Это явно отмечено, а не скрыто суммой PASS.

Следующая задача перед freeze — получить полный актуальный список исходных тестов и последний подтверждённый QA harness, сопоставить с PD-картой и выполнить доступное недостающее покрытие на exact кандидате. Не добавлять новые функции без конкретного выявленного дефекта. Не поручать владельцу переносить QA-файлы. Независимая приёмка Codex идёт отдельно только после готовности точного кандидата и исполнимой карты.

## 7. Внешние материалы

Повторно прочитаны официальные limits, WebSearchAsync.Search, Operation.Get, pricing, authentication и web-search instructions Яндекса; Chrome network requests и worker lifecycle. Полная цепочка SOURCE -> WHAT SUPPORTS -> PROJECT BOUNDARY -> ACTION сохранена в B12_SOURCE_TRACE.json внутри архива.

Ограничения extension (1500 items, 25 steps, export 16 MiB, local time budget) не выдаются за правила Яндекса. Цена остаётся оценкой, а не счётом; срок хранения не становится выдуманным точным expires_at. Локальные ограничения скорости/бюджета не объявлены account-wide контролем hourly quota во всех приложениях.

## 8. Итог и запрет выпуска

Новых проверок процесса Chrome/DOM/MV3/настоящей IDB и памяти не было. Ранее заблокированный браузер повторно не запускался, политики не обходились. Operation-host не включён, живой ключ не проверялся. Installable ZIP не создавался.

**RELEASE_ALLOWED = NO. INDEPENDENT_PRE_DELIVERY_GATE = NOT_RUN. OWNER_INSTALLABLE_HANDOFF = FORBIDDEN.**

Простыми словами: команды и документация сверены, лишний код не добавлялся. Доступная повторная проверка прошла. Зафиксирован точный оставшийся объём приёмки, который нельзя заменить количеством уже пройденных отдельных тестов.
