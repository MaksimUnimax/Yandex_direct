name: Persist MK01 client report PDF

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-persist:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo manual workflow disabled after failed cross-platform reconstruction
